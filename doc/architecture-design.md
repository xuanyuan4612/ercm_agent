# 赫尔墨斯（Hermes）系统架构设计文档

---

## 一、架构概述

### 1.1 系统定位

赫尔墨斯（Hermes）是面向科沃斯集团的风险控制 AI 智能体系统，基于大语言模型（LLM）+ 检索增强生成（RAG）+ 多智能体协作（A2A）技术构建。系统覆盖风控业务全流程，集成**八大核心功能模块**，形成"**风险发现 → 调查分析 → 处置追责 → 整改闭环**"的完整风控链路。

**最终生产目标规模**：支撑集团 **10,000 名注册/授权内部用户**、峰值 **500 并发会话/API 请求并发**，管理约 **10TB 文档与证据数据**，处理音频、视频、图像、PDF/Office 等多模态材料；API P95 < 500ms（不含 LLM/异步任务执行时间），满足等保二级、强审计、跨系统集成和长期灾备要求。

#### 生产架构边界与权威职责

本文档描述最终生产架构，不以 MVP、PoC 或分阶段交付为口径。所有模块必须遵守统一的 Workflow、Agent、Tool、Event、Audit 契约，避免 API、Worker、Agent 和外部系统各自推进业务状态。

| 权威域                            | 生产职责                                                                                | 禁止事项                        |
| ------------------------------ | ----------------------------------------------------------------------------------- | --------------------------- |
| **LangGraph Workflow Runtime** | 唯一工作流状态推进中心；负责阶段路由、HITL 暂停/恢复、重试、人工接管和下游事件触发                                        | API、Worker、Agent 不得直接跳转业务阶段 |
| **PostgreSQL 业务事实库**           | 存储案件、审批、任务、审计、Outbox/Inbox、权限等业务事实                                                  | 不承担 10TB 级全文/向量检索主负载        |
| **检索与知识域**                     | Elasticsearch/OpenSearch 承载全文检索；Milvus Distributed 承载大规模向量检索；PostgreSQL 存知识元数据和索引版本 | 业务 OLTP 与大规模向量召回不得共库承压      |
| **RabbitMQ + Celery**          | 可靠任务分发、异步执行、重试、DLQ、队列积压治理                                                           | 不承载工作流语义，不裁决案件终态            |
| **Agent 与模型能力域**               | 生成结构化建议、证据摘要、知识引用和下一步建议                                                             | 不直接修改业务终态，不绕过 HITL 执行高风险动作  |
| **事件集成域**                      | 通过 Adapter、Outbox、Inbox、Webhook 签名、幂等键和审计实现外部系统协作                                   | 不允许直接数据库互写或无审计回调            |

### 1.2 核心架构能力

| 维度 | 能力描述 |
|------|----------|
| **工作流编排** | LangGraph 作为唯一 Workflow Runtime，使用 durable checkpointer 持久化 workflow state；HITL、驳回重跑、人工修改、恢复和阶段推进均由工作流语义管理 |
| **知识增强** | RAG 由统一 RAG Orchestrator 编排：Elasticsearch/OpenSearch 全文召回 + Milvus 向量召回 + metadata filter + rerank + 引用追溯；pgvector 仅作为轻量/局部检索能力 |
| **多智能体协作** | A2A 被定义为企业事件集成协议，包含消息信封、schema_version、trace_id、idempotency_key、callback_url、签名、Outbox/Inbox 和回调确认 |
| **外部系统集成** | 风控、OA/BPM、MDM、HR、法务、财务系统通过 Adapter 接入；所有外发事件先写 Outbox，所有回调先写 Inbox 并完成验签、去重、审计 |
| **异步优先架构** | RabbitMQ quorum queues + Celery Worker 承担长耗时任务，Worker 写任务结果并发布完成事件，由 Workflow Runtime 恢复流程；Worker 不直接推进业务阶段 |
| **多模态处理管道** | 独立处理管线：音频（Whisper ASR + 说话人分离）、图像（PaddleOCR + CLIP 分类）、视频（OpenCV 关键帧 + 场景分析）、文档（unstructured.io PDF/Office 解析）；GPU 加速处理 |
| **数据分层存储** | PostgreSQL 存业务事实与审计事实；Redis 做缓存、限流、会话等热路径；MinIO 存对象与报告；Elasticsearch/OpenSearch 和 Milvus 独立承载检索负载；冷数据归档到 NAS/磁带 |
| **水平弹性伸缩** | 所有服务均无状态设计，基于 Kubernetes 自动扩缩容；API 层 3-20 实例（HPA）；Worker 按任务类型分 9 个独立池独立扩缩 |
| **CDN 与缓存** | Nginx 缓存层 + Redis Cluster 提供文档预览/缩略图 CDN 分发；热点查询缓存加速（5min TTL）；视频 HLS 自适应码率分段流 |
| **可观测性** | OpenTelemetry 统一 trace/metrics/logs 接入标准，trace_id 贯穿 API、Workflow、Worker、LLM、RAG、Tool、外部系统；Prometheus/Grafana、LangFuse、Jaeger/Tempo、structlog 统一治理 |
| **分层记忆** | L1 感知记忆 (即时上下文) → L2 会话记忆 (多轮对话) → L3 案例记忆 (案件全生命周期) → L4 组织记忆 (跨案件知识沉淀)。记忆从短到长逐层固化，支持跨案件学习与经验复用 |

### 1.3 系统边界

```
┌───────────────────────────────────────────────────────────────────────┐
│                        赫尔墨斯（Hermes）系统边界                          │
│                                                                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │  廉洁监察     │  │  风险监控     │  │  内控评价     │  │  商业秘密     │  │
│  │ (反舞弊调查)  │  │ (主动扫描)   │  │ (合规评价)   │  │ (保密管理)   │  │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  │
│         │                │                │                │          │
│  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴──────┐  │
│  │  专项审计     │  │  离任审计     │  │  行为风险     │  │  持续改善     │  │
│  │ (专项检查)   │  │ (离职审查)   │  │ (行为分析)   │  │ (整改跟踪)   │  │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘  │
│                                                                       │
│  ┌─────────────────────── 共享基础设施 ──────────────────────────────┐ │
│  │  认证授权(RBAC) │ 审计日志 │ 知识库(RAG) │ 多模态处理 │ 文档生成   │ │
│  │  通知服务 │ 数据分层存储 │ 搜索服务(Elasticsearch) │ CDN/缓存   │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│  ┌───────────────── 多模态数据接入 ──────────────────────────────────┐ │
│  │  音频(访谈录音/语音备忘录) │ 图像(证据照片/扫描件)                    │ │
│  │  视频(监控录像/现场勘察)   │ 文档(PDF合同/报告/证据材料)              │ │
│  │  → 异步处理管道 → 文本提取/OCR/标签分类 → 知识库索引 → 全文检索      │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│  ┌─────────────────── 外部集成边界 ───────────────────────────────────┐ │
│  │  风控系统  │  OA系统(BPM) │  MDM  │  龟宝(A2A) │ 西塞罗(A2A) │ 波特(A2A) │
│  └──────────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 二、设计原则

### 2.1 架构原则

| 原则 | 说明 | 落地方式 |
|------|------|----------|
| **关注点分离 (SoC)** | 业务模块独立部署，共享基础设施横向复用 | 8 个业务模块各自维护独立的 LangGraph 子图；共享认证、知识库、审计日志等横切服务 |
| **人机协同 (HITL)** | 每个工作流阶段碳基守门，AI 辅助决策、人工最终确认 | LangGraph `interrupt_before` 机制在每个阶段节点前挂起等待人工审批 |
| **有状态工作流** | 支持案件断点续跑、阶段回溯、人工介入后恢复 | PostgreSQL durable checkpointer 或等价持久化方案保存 workflow state；Redis 仅作为热缓存 |
| **知识驱动** | 各阶段决策基于结构化知识库和证据引用，而非仅依赖 LLM 参数记忆 | RAG Orchestrator 编排 Elasticsearch/OpenSearch、Milvus、metadata filter、rerank 和引用追溯 |
| **协议解耦** | 外部系统通过 Adapter + 事件契约隔离，不直接耦合 | 风控、OA/BPM、MDM、A2A 均走 Outbox/Inbox、签名、幂等和审计 |
| **安全内建 (Security by Design)** | 等保二级要求嵌入架构设计而非事后修补 | JWT + RBAC + AES-256-GCM 字段加密 + 全量审计日志 + 软删除 |
| **弹性设计** | 关键节点失败自动重试，不可恢复时降级至人工接管 | Workflow 节点定义 max_retries/timeout；RabbitMQ 负责任务重投递和 DLQ；失败恢复由 Workflow Runtime 裁决 |
| **异步优先 (Async-First)** | 所有重计算操作异步执行，用户 API 返回命令受理结果或 task_id | Worker 完成后写任务结果并发布事件；Workflow Runtime 消费事件后恢复节点 |
| **数据分层 (Data Tiering)** | 按数据访问频率、负载类型和生命周期分层存储 | 业务事实库、全文检索库、向量检索库、对象存储、缓存和冷归档物理隔离，避免单一数据库承载所有负载 |
| **水平扩展 (Horizontal Scale)** | 所有服务无状态，独立水平扩展 | API 层：K8s HPA 自动扩缩 3-20 实例；Worker 层：按任务类型独立 Pod 池扩缩；数据库：主从读写分离 + 2 读副本 |
| **多模态管道 (Multimodal Pipeline)** | 每种媒体类型独立处理管线，充分利用 GPU 加速，处理结果统一索引 | 音频管道（Whisper + 说话人分离）；图像管道（PaddleOCR + CLIP 分类）；视频管道（OpenCV 关键帧 + 场景分析）；文档管道（unstructured.io PDF/Office 解析） |
| **渐进加载 (Progressive Loading)** | 大文件不阻塞用户交互，先预览后加载 | 文档缩略图预生成 + 分页流式加载；视频 HLS 分段传输；音频流式播放；文件 Range 分段下载 |
| **统一生产契约** | 8 大模块全部遵守同一套 Workflow、Agent、Tool、Event、Audit 契约 | 新模块只扩展业务 Graph 和 Agent，不改变底层运行语义和审计规则 |

### 2.2 关键架构决策 (ADR)

| ADR # | 决策 | 理由 | 替代方案（已否决） |
|-------|------|------|-------------------|
| ADR-001 | 使用 LangGraph 而非自研工作流引擎 | 原生支持 Checkpointer、interrupt、条件路由、streaming；LangChain 生态集成 | 自研状态机（维护成本高）；Temporal（过重，不适合 LLM 工作流） |
| ADR-002 | 业务事实库与知识检索库物理隔离 | 10TB 文档和大规模知识库场景下，OLTP、审计、审批与向量召回共库会放大锁、IO、备份和恢复风险；生产采用 PostgreSQL 业务库 + Elasticsearch/OpenSearch 全文检索 + Milvus Distributed 向量检索 | 单一 PostgreSQL + pgvector 承载全部负载（运维简单但生产风险高）；外部 SaaS 向量库（数据出域风险） |
| ADR-003 | RabbitMQ + Celery 替换原有 Celery + Redis 直接架构 | 10K 注册用户/峰值 500 并发下需要可靠的消息持久化、quorum queues、消息优先级、死信队列（DLX）、联邦支持；RabbitMQ 提供企业级可靠性 | Redis 直接做队列（持久化弱，无原生优先级）；Kafka（日志语义，不适合任务队列场景） |
| ADR-004 | Model Gateway + Provider Adapter | DeepSeek、Qwen 和私有模型 endpoint 均通过 OpenAI-compatible 或 provider adapter 接入；模型路由、熔断、灰度、成本和质量评估由 Model Registry + Routing Policy 管理 | 在代码和文档中硬编码某个模型版本 |
| ADR-005 | 知识库离线本地管理，无飞书 API 依赖 | 等保要求数据不出内网；离线管理满足知识库低频更新场景 | 飞书 API 同步（外部网络依赖） |
| ADR-006 | Elasticsearch 9.x / OpenSearch 作为全文检索域 | 10TB 文档规模下，PostgreSQL FTS 不应承担主全文检索；生产默认 Elasticsearch 9.x，若企业许可或开源策略限制则切换 OpenSearch，二者均隐藏在 Search Adapter 后 | PostgreSQL FTS 作为主检索（扩展性不足）；直接在业务代码耦合某一搜索产品 |
| ADR-007 | PaddleOCR 为中文 OCR 引擎（自部署） | 中文场景准确率 > Tesseract；支持表格识别、印章检测、版面分析；GPU 加速；纯内网部署，满足等保要求 | Tesseract（中文弱）；百度 OCR API（外部依赖） |
| ADR-008 | Whisper large-v3（自部署）为语音转文字引擎 | 开源、多语言支持、GPU 加速、可批量离线处理；1 小时音频 < 5 分钟处理（V100 GPU） | 阿里云语音识别（外部依赖、按量计费）；讯飞（API 限额） |
| ADR-009 | Kubernetes 作为最终生产运行平台 | 10K 注册用户、峰值 500 并发和 10TB 数据规模需要自动扩缩容、滚动更新、健康检查、资源调度和 Operator 治理；Docker Compose 仅属于非生产辅助工具 | Docker Swarm（功能弱）；Nomad（生态小） |
| ADR-010 | OpenTelemetry 作为统一可观测性标准 | FastAPI、Celery、LangGraph、LLM、RAG、Tool 和外部系统必须共享 trace_id；Jaeger/Tempo 只是 trace backend，可替换但采集标准统一 | 各组件各自打日志和 trace，无法串联端到端链路 |
| ADR-011 | PostgreSQL durable checkpointer 而非 Redis 唯一 checkpointer | HITL 可能暂停数小时到数周，workflow state 必须具备数据库级持久化、备份、恢复和审计关联；Redis 只做热路径缓存 | Redis Cluster 作为唯一 workflow state 权威（恢复和审计风险） |

### 2.3 生产架构关键修正

| 修正项 | 生产落地口径 |
|--------|--------------|
| **Redis Checkpointer 改为 durable checkpointer** | 生产 HITL 必须能长期暂停和恢复，PostgreSQL durable checkpointer 为权威；Redis 只做热路径缓存和临时加速。 |
| **pgvector 从业务库中拆出** | 10TB 文档量下，业务 OLTP、审计、审批和向量召回不得共库承压；Milvus Distributed 承载大规模向量检索，pgvector 只保留轻量场景。 |
| **A2A 改成企业事件集成架构** | A2A 必须具备消息信封、schema_version、trace_id、idempotency_key、callback_url、签名、Outbox、Inbox、回调确认和审计链。 |
| **Celery 不承担工作流语义** | Celery 是任务执行器，RabbitMQ 是消息传输层，LangGraph 才是流程状态机；Worker 完成后发布事件，由 Workflow Runtime 恢复节点。 |
| **OpenTelemetry 优先** | trace_id 贯穿 API、Workflow、Worker、LLM、RAG、Tool、外部系统；Jaeger/Tempo、Prometheus、日志和 LangFuse 都以同一 trace 关联。 |
| **LLM 不硬编码模型名** | DeepSeek、Qwen、私有模型 endpoint 均进入 Model Gateway；模型版本、路由、熔断、灰度、成本和质量评估由 Model Registry 管理。 |

---

## 三、系统分层架构

### 3.1 最终生产主架构：6 个主运行层 + 3 个横切治理面

Hermes 的最终生产架构不把所有能力都堆成纵向分层，而是拆成 **6 个主运行层** 和 **3 个横切治理面**。主运行层描述一次业务请求、工作流推进、Agent 分析、检索、异步任务、数据落库的真实运行链路；横切治理面贯穿所有运行层，负责安全、可观测性、质量、平台和交付治理。

这种表达更接近企业生产项目的实际组织方式：研发团队按运行链路拆服务和模块，安全、观测、平台团队按治理面建立统一标准，避免"层数很多但边界重复"。

```mermaid
flowchart TB
    subgraph RUN["主运行链路：6 个主运行层"]
        A["1.接入与业务应用层<br/>Vue SPA / FastAPI / WebSocket / Webhook / Ingress / WAF / SSO<br/>案件 / 审批 / 任务中心 / 文档 / 权限 / 审计查询"]
        B["2.工作流运行层<br/>LangGraph Runtime / durable checkpointer / HITL interrupt+resume"]
        C["3.Agent 与模型能力层<br/>Model Gateway / Provider Adapter / Prompt Manager / Tool Registry / Memory / Evaluator"]
        D["4.知识检索与文档智能层<br/>RAG Orchestrator / Elasticsearch or OpenSearch / Milvus / OCR / ASR / rerank"]
        E["5.异步任务与事件集成层<br/>RabbitMQ quorum queues / Celery Worker / Outbox / Inbox / A2A Adapter / DLQ / KEDA"]
        F["6.数据与存储层<br/>PostgreSQL business facts + checkpoint / Redis hot cache / MinIO / NAS archive"]
    end

    subgraph GOV["横切治理面：贯穿所有运行层"]
        G1["安全与权限治理<br/>SSO / RBAC / RLS / encryption / Vault or KMS / presigned URL / audit"]
        G2["可观测性与质量治理<br/>OpenTelemetry / Prometheus / Grafana / Jaeger or Tempo / LangFuse / trace_id"]
        G3["平台与交付治理<br/>Kubernetes / Operators / Helm / Argo CD or GitLab CI / SBOM / backup drill"]
    end

    A --> B
    B --> C
    C --> D
    B --> E
    E --> B
    B --> F
    C --> F
    D --> F
    E --> F
    RUN -. governed by .-> G1
    RUN -. observed by .-> G2
    RUN -. delivered by .-> G3
```

### 3.2 主运行层职责与状态权威

| 主运行层 | 生产职责 | 状态权威 | 关键技术 |
|----------|----------|----------|----------|
| **接入与业务应用层** | 前端交互、REST/WebSocket/Webhook 接入、TLS/WAF/SSO 入口、权限校验、业务命令受理、查询聚合、审批界面、任务中心、文档管理和审计查询 | PostgreSQL 中的业务事实；本层不直接推进 workflow 阶段 | Vue 3、FastAPI ASGI、Ingress/Nginx、企业 SSO/OIDC、SQLAlchemy async、RBAC |
| **工作流运行层** | 唯一流程推进中心；负责阶段路由、HITL、重试、恢复、人工接管、节点重跑和下游事件触发 | LangGraph durable checkpoint + 业务表关联 | LangGraph、PostgreSQL checkpointer、interrupt/resume |
| **Agent 与模型能力层** | 结构化建议生成、证据摘要、知识引用、Tool 编排、模型路由、熔断、灰度、成本和质量评估 | Agent 调用记录、Prompt/模型版本和结构化 stage_output | Model Gateway、Provider Adapter、Model Registry、Prompt Manager、Tool Registry、LangFuse |
| **知识检索与文档智能层** | 文档解析、OCR、ASR、全文召回、向量召回、metadata filter、rerank、引用追溯和知识版本管理 | Search index + vector index + PostgreSQL knowledge metadata | Elasticsearch 9.x/OpenSearch、Milvus Distributed、unstructured.io、PaddleOCR、Whisper、Reranker |
| **异步任务与事件集成层** | 长耗时任务执行、消息可靠投递、重试、DLQ、任务状态、队列扩缩容、Outbox/Inbox、外部系统 Adapter、A2A 消息信封、签名、幂等和回调确认 | task_state、event_outbox、event_inbox、外部同步日志；不裁决业务终态 | RabbitMQ quorum queues、Celery、KEDA、Adapter、Webhook、HMAC |
| **数据与存储层** | 业务事实、审计事实、workflow checkpoint、对象文件、报告、缓存、归档、备份和恢复 | PostgreSQL、MinIO、Redis、NAS/归档存储；PostgreSQL 是业务事实和 checkpoint 权威 | PostgreSQL 18/17/16、CloudNativePG/Patroni、Redis Cluster、MinIO Operator、PgBouncer、WAL 归档 |

### 3.2.1 横切治理面

| 横切治理面 | 覆盖范围 | 生产落地要求 | 关键技术 |
|------------|----------|--------------|----------|
| **安全与权限治理** | API、Workflow、Agent、RAG、对象存储、外部回调、运维入口 | SSO、RBAC、租户隔离、PostgreSQL RLS、敏感字段列级加密、对象服务端加密、短期预签名 URL、Webhook 验签、密钥托管、append-only 审计 | 企业 SSO/OIDC、RBAC、RLS、Vault/KMS、External Secrets Operator、HMAC、audit_log 分区表 |
| **可观测性与质量治理** | API、Workflow、Worker、LLM、RAG、Tool、外部系统调用 | trace_id 贯穿全链路；统一采集 logs/metrics/traces；监控 LLM token、Prompt 版本、RAG 命中、Agent 通过率、人工驳回率和成本 | OpenTelemetry、Prometheus、Grafana、Jaeger/Tempo、LangFuse、structlog |
| **平台与交付治理** | 应用服务、状态型组件、发布流水线、镜像、依赖、备份恢复 | Kubernetes 作为生产运行平台；状态型组件优先 Operator；Helm/GitOps 管理部署；镜像扫描、SBOM、漏洞治理、容量压测和灾备演练纳入门禁 | Kubernetes、CloudNativePG/Patroni、ECK/OpenSearch Operator、MinIO Operator、Prometheus Operator、Helm、Argo CD/GitLab CI |

### 3.3 核心运行契约

1. API 收到请求后，只创建业务命令、业务事实或 workflow 实例，不直接跳阶段。
2. Workflow Runtime 读取业务上下文，调用 Agent 或派发异步任务。
3. Agent 通过 RAG 和 Tool Registry 生成结构化阶段输出，不直接修改案件终态。
4. 长任务进入 RabbitMQ，由 Celery Worker 执行；Worker 写任务结果并发布完成事件。
5. Workflow Runtime 消费任务完成事件并恢复对应节点。
6. 高风险节点通过 LangGraph interrupt 暂停，等待人工审批。
7. 人工审批写入 approval、audit_log，并把修改后的内容回写 workflow state。
8. Workflow 继续推进，必要时通过 Outbox 通知外部系统。
9. 外部系统回调进入 Inbox，验签、去重、审计后恢复流程。
10. 案件关闭后生成归档、整改问题、管理报告和可追溯审计链。

### 3.4 物理组件拓扑参考图

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│                             呈现层 (Presentation Layer)                             │
│  ┌──────────────────────────────┐   ┌──────────────────────────────────────────┐  │
│  │   Vue 3 + TypeScript SPA     │   │   风控系统（嵌入式 Web 按钮集成）           │  │
│  │   ├── 案件工作台              │   │   ├── 廉洁监察管理-舞弊处理页              │  │
│  │   ├── 守门工作台              │   │   ├── 内控评价-作业模块                   │  │
│  │   ├── 任务中心                │   │   └── 商业秘密管理-信息档案台账             │  │
│  │   ├── 多模态证据查看器         │   └──────────────────────────────────────────┘  │
│  │   │  (音频播放+时间轴/视频     │                                                │
│  │   │   HLS预览/图片对比        │                                                │
│  │   │   /PDF分页流式加载)       │                                                │
│  │   ├── 文档预览/划词调整        │                                                │
│  │   └── 管理后台(RBAC)          │                                                │
│  └──────────────┬───────────────┘                                                │
│                 │  HTTPS + WebSocket (wss)                                       │
├─────────────────┼─────────────────────────────────────────────────────────────────┤
│                        CDN & 缓存层 (CDN & Cache Layer)                             │
│  ┌──────────────────────────────────────────────────────────────────────────┐     │
│  │  Nginx 反向代理 + SSL 终结 + 静态资源 + 速率限制 + 文件预览缓存              │     │
│  │  ├── Vue SPA 静态资源 (长期缓存, Cache-Control: max-age=31536000)          │     │
│  │  ├── 文档缩略图/预览图 (CDN 缓存, 24h TTL)                                  │     │
│  │  ├── 音频/视频分段流 (Range 请求代理, HLS playlist 缓存)                    │     │
│  │  ├── API 反向代理 → api-svc:8000 (负载均衡, least_conn)                     │     │
│  │  └── WebSocket 升级代理 (sticky session via ip_hash)                        │     │
│  └──────────────────────────────────────────────────────────────────────────┘     │
├──────────────────────────────────────────────────────────────────────────────────┤
│                           网关层 (Gateway Layer) - API 接入                         │
│  ┌──────────────────────────────────────────────────────────────────────────┐     │
│  │  FastAPI REST API Pool (K8s Deployment, HPA 3-20 replicas)                │     │
│  │  ├── /api/v1/cases       ├── /api/v1/workflow  ├── /api/v1/approval       │     │
│  │  ├── /api/v1/docs        ├── /api/v1/kb         ├── /api/v1/webhooks      │     │
│  │  ├── /api/v1/media       ├── /api/v1/tasks      ├── /api/v1/search (ES)   │     │
│  │  ├── /api/v1/stream (SSE/WebSocket 异步结果推送)                            │     │
│  │  └── Service Layer: CaseService | WorkflowService | DocService | MediaService│    │
│  └──────────────────────────────────────────────────────────────────────────┘     │
├──────────────────────────────────────────────────────────────────────────────────┤
│                        LangGraph 工作流引擎层 (Workflow Engine)                      │
│  ┌──────────────────────────────────────────────────────────────────────────┐     │
│  │  StateGraph (每个模块独立 Graph 定义，8 个顶层 Graph + 子图)               │     │
│  │  ├── 廉洁监察 Graph (6 节点，含条件路由)    ├── 专项审计 Graph (5 节点)     │     │
│  │  ├── 风险监控 Graph (6 节点)                ├── 离任审计 Graph (6 节点)     │     │
│  │  ├── 内控评价 Graph (13 节点)               ├── 商业秘密 Graph (3 功能)     │     │
│  │  ├── 行为风险 Graph (2 功能)                └── 持续改善 Graph (5 子模块)   │     │
│  │  ├── Durable Checkpointer (PostgreSQL) + Redis 热缓存 ←→ 断点续跑 / 状态恢复 │     │
│  │  └── interrupt_before 机制  ←→  碳基守门 (HITL)                            │     │
│  └──────────────────────────────────────────────────────────────────────────┘     │
├──────────────────────────────────────────────────────────────────────────────────┤
│                         多模态处理层 (Multimodal Processing Layer)                  │
│  ┌──────────────────────────────────────────────────────────────────────────┐     │
│  │  文件上传 (MinIO → RabbitMQ 事件触发)                                       │     │
│  │                                                                           │     │
│  │  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐           │     │
│  │  │  音频管道        │  │  图像管道        │  │  视频管道        │           │     │
│  │  │                 │  │                 │  │                 │           │     │
│  │  │ Whisper (ASR)   │  │ PaddleOCR       │  │ OpenCV 关键帧    │           │     │
│  │  │ + 说话人分离     │  │ + 图像分类(CLIP) │  │ + 场景分析       │           │     │
│  │  │ + 时间轴标注     │  │ + 印章/签名检测  │  │ + 元数据提取     │           │     │
│  │  │ → 结构化文本    │  │ → 结构化文本     │  │ → 帧序列+描述    │           │     │
│  │  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘           │     │
│  │           │                    │                    │                     │     │
│  │  ┌────────┴────────────────────┴────────────────────┴────────┐           │     │
│  │  │                   文档管道 (Document Pipeline)               │           │     │
│  │  │  unstructured.io / LlamaParse → PDF/Word/Excel/PPT 解析      │           │     │
│  │  │  ├── 文本提取 + 表格抽取 + 层次结构识别                      │           │     │
│  │  │  ├── 章节切分 (Chunking) → Embedding → Milvus 索引          │           │     │
│  │  │  └── 缩略图预生成 → MinIO + CDN                               │           │     │
│  │  └─────────────────────────────────────────────────────────────┘           │     │
│  │                                                                           │     │
│  │  所有处理结果 → Elasticsearch/OpenSearch 全文索引 + Milvus 向量索引             │     │
│  └──────────────────────────────────────────────────────────────────────────┘     │
├──────────────────────────────────────────────────────────────────────────────────┤
│                        Agent 核心层 (LLM 推理 + RAG + 搜索)                         │
│  ┌──────────────────────────────────────────────────────────────────────────┐     │
│  │  ┌──────────────┐  ┌───────────────┐  ┌───────────────┐  ┌───────────┐  │     │
│  │  │ LLM Adapter  │  │ RAG Engine    │  │ Tool System   │  │ Prompt    │  │     │
│  │  │ Model Gateway│  │ Milvus        │  │ 语音转文字     │  │ Manager   │  │     │
│  │  │ 多Provider   │  │ 向量集合       │  │ 文档解析      │  │ YAML模板  │  │     │
│  │  │ LangFuse追踪 │  │ Embedding     │  │ SQL 分析      │  │ 版本化     │  │     │
│  │  │ 优先级队列    │  │ Re-ranking    │  │ 图像识别      │  │ 热加载    │  │     │
│  │  │ 速率限制     │  │ 混合检索(ES)  │  │ ES 全文检索    │  └───────────┘  │     │
│  │  └──────────────┘  └───────────────┘  │ 报告生成      │                  │     │
│  │                                        └───────────────┘                  │     │
│  └──────────────────────────────────────────────────────────────────────────┘     │
├──────────────────────────────────────────────────────────────────────────────────┤
│                           集成层 (Integration Layer)                                │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐  ┌──────────────────┐   │
│  │ 风控 Adapter   │  │ OA Adapter    │  │ MDM Adapter   │  │    A2A Bus       │   │
│  │ 字段映射       │  │ BPM 审批集成  │  │ 黑名单同步     │  │ 龟宝│西塞罗│波特 │   │
│  │ 状态同步       │  │ 处罚公告推送   │  │ 主数据查询     │  │ RabbitMQ 驱动    │   │
│  │ WebSocket 推送 │  │ 审批结果回调   │  │ 组织架构查询   │  │ 统一协议│回调     │   │
│  └───────────────┘  └───────────────┘  └───────────────┘  └──────────────────┘   │
├──────────────────────────────────────────────────────────────────────────────────┤
│                         中间件层 (Middleware Layer)                                  │
│  ┌─────────────┐  ┌─────────────────┐  ┌──────────────┐  ┌───────────────────┐   │
│  │ Auth Middle │  │ Audit Middle    │  │ RBAC Filter  │  │ Rate Limiter      │   │
│  │ JWT 验证    │  │ 全量操作日志     │  │ 角色行级过滤  │  │ 混合限流          │   │
│  │ 黑名单检查   │  │ 不可篡改         │  │ 数据脱敏     │  │ 全局: 1000/s     │   │
│  │ Refresh流转 │  │ traceId 注入     │  │ 字段级权限    │  │ 用户: 100/min    │   │
│  └─────────────┘  └─────────────────┘  └──────────────┘  │ LLM: 令牌桶 50/s  │   │
│                                                          └───────────────────┘   │
├──────────────────────────────────────────────────────────────────────────────────┤
│                    数据分层存储层 (Tiered Data Storage Layer)                        │
│                                                                                   │
│  ┌─────────── 业务事实库: PostgreSQL 18/17/16 HA Cluster ────────────────────┐   │
│  │  Patroni Primary + 2 Read Replicas | PgBouncer 连接池 (pool_size=500)     │   │
│  │  案件/审批/任务 | 用户/权限 | RBAC配置 | Outbox/Inbox | 审计日志 | 表分区   │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│  ┌─────────── 热缓存层 (Hot Cache): Redis 7 Cluster ─────────────────────────┐   │
│  │  3 主 3 从集群 | Session | 限流计数 | 热点缓存 | checkpoint 热缓存     │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│  ┌─────────── 温层 (Warm): MinIO 多节点集群 ─────────────────────────────────┐   │
│  │  4 节点 EC 12+4, ~20TB | 附件 | 文档/报告 | 音视频 | 缩略图(CDN源)       │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│  ┌─────────── 冷层 (Cold): NAS / 磁带归档 ───────────────────────────────────┐   │
│  │  历史案件(>2年) | 保留10年 | 元数据索引(ES) | 按需恢复(SLA < 24h)         │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│  ┌─────────── 全文检索: Elasticsearch 9.x / OpenSearch 集群 ────────────────┐   │
│  │  全量文档全文索引 | 中文分词 | 审计日志(1年可搜索) | Search Adapter       │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│  ┌─────────── 向量检索: Milvus Distributed ────────────────────────────────┐   │
│  │  知识库/证据向量召回 | metadata filter | rerank 前候选集 | 模块集合隔离     │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│  ┌─────────── 消息队列: RabbitMQ 3 节点 quorum queues 集群 ─────────────────┐   │
│  │  audio/img/video/doc/report/sync/a2a/llm/kb 9个队列 + DLX死信队列        │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│  ┌─────────── Celery Worker Pool (按队列分池, 独立K8s Deployment) ───────────┐   │
│  │  音频Worker(GPU,1-3pod) | 图像(GPU,1-3) | 视频(GPU,1-2) | 文档(CPU,2-5) │   │
│  │  报告(CPU,2-4) | 同步(CPU,1-3) | A2A(CPU,1-3) | KB(CPU,1-3) | LLM(1-5) │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────────┘
                                      │
├──────────────────────────────────────┼────────────────────────────────────────────┤
│                         可观测性层 (Observability Layer)                             │
│  ┌────────────────┐  ┌──────────────────┐  ┌──────────────────┐  ┌────────────┐   │
│  │  LangFuse       │  │  Prometheus      │  │  structlog        │  │  Jaeger    │   │
│  │  + Grafana      │  │  + Grafana       │  │                  │  │  + Grafana │   │
│  │  ├── Token 消耗  │  │  ├── API QPS/P95  │  │  ├── 结构化JSON   │  │  ├── 跨服务│   │
│  │  ├── LLM 延迟    │  │  ├── DB 连接池    │  │  ├── traceId 关联 │  │  │  调用链 │   │
│  │  ├── Tool 调用   │  │  ├── Redis 命中率  │  │  └── 敏感字段脱敏 │  │  ├── 耗时  │   │
│  │  ├── KB 检索命中 │  │  ├── RabbitMQ 深度│  │                  │  │  │  分析  │   │
│  │  └── Prompt 版本  │  │  ├── K8s Pod 资源 │  │                  │  │  └── 采样  │   │
│  └────────────────┘  │  ├── GPU 利用率    │  └──────────────────┘  └────────────┘   │
│                       │  ├── ES 查询延迟   │                                          │
│                       │  └── 业务仪表板     │                                          │
│                       │      (用户活跃/案件  │                                          │
│                       │       处理量/存储    │                                          │
│                       │       使用趋势等)    │                                          │
│                       └──────────────────┘                                          │
└──────────────────────────────────────────────────────────────────────────────────┘
```

### 3.5 物理组件职责补充

| 层级 | 职责 | 关键组件 | 对外依赖 |
|------|------|----------|----------|
| **呈现层** | 用户交互界面，多模态证据查看器，内嵌风控系统 Web 组件 | Vue 3 SPA, Element Plus, Tiptap, HLS.js, PDF.js, Wavesurfer.js | Nginx 网关, CDN |
| **CDN & 缓存层** | SSL 终结、静态资源分发、文档预览缓存、API 负载均衡、速率限制 | Nginx (含 ngx_http_slice_module, least_conn) | — |
| **网关层** | RESTful API 暴露、WebSocket 推送、Webhook 接收、文件上传代理 | FastAPI Router Pool (K8s HPA), JWT 中间件 | Service 层, RabbitMQ |
| **工作流引擎层** | 工作流调度、状态管理、碳基守门流转 | LangGraph StateGraph, PostgreSQL durable checkpointer, Redis 热缓存 | Agent 核心层, 异步任务层 |
| **多模态处理层** | 音频/图像/视频/文档的 AI 处理、文本提取、结构化转换 | Whisper, PaddleOCR, CLIP, OpenCV, unstructured.io | MinIO, RabbitMQ, GPU |
| **Agent 核心层** | LLM 推理、知识检索、Tool 调用、Prompt 管理、结构化输出 | Model Gateway, RAG Orchestrator, Milvus, Elasticsearch/OpenSearch, Tool Registry | 工作流层, 检索层, 多模态处理层 |
| **集成层** | 外部系统协议适配、A2A 企业事件通信 | System Adapters, A2A Bus, Outbox/Inbox, Webhook 签名 | 外部系统 |
| **中间件层** | 横切关注点：认证、审计、权限过滤、限流 | FastAPI Middleware Stack | — |
| **数据分层存储层** | 业务事实、审计、全文索引、向量索引、缓存、文件、冷归档、消息队列、Worker Pool | PostgreSQL HA, Redis Cluster, MinIO, NAS/磁带, Elasticsearch/OpenSearch, Milvus, RabbitMQ, Celery Worker Pool | — |
| **可观测性层** | LLM 调用追踪、系统指标、分布式追踪、业务日志 | OpenTelemetry, LangFuse, Prometheus, structlog, Jaeger/Tempo, Grafana | — |

### 3.6 多模态处理管道详情

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│  用户上传文件 → MinIO 接收 → 事件触发 → RabbitMQ → Worker 处理 → 结果写入         │
│                                                                                  │
│  ┌─ 音频管道 (Audio Pipeline) ──────────────────────────────────────────────┐   │
│  │  @audio_queue (GPU Node, 1-3 pods)                                        │   │
│  │  原始音频(.mp3/.wav/.m4a)                                                  │   │
│  │  ├─[Whisper large-v3] → 语音转文字 (含时间戳)                               │   │
│  │  ├─[pyannote.audio]   → 说话人分离 (Speaker Diarization)                    │   │
│  │  ├─[语言检测]         → 自动识别中/英/混合                                   │   │
│  │  └─后处理: 结构化转录JSON → 全文索引 + Milvus 向量索引                       │   │
│  │  性能: 1小时音频 → < 5 分钟 (V100 GPU)                                      │   │
│  └────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  ┌─ 图像管道 (Image Pipeline) ──────────────────────────────────────────────┐   │
│  │  @image_queue (GPU Node, 1-3 pods)                                        │   │
│  │  原始图像(.jpg/.png/.tiff/.bmp)                                            │   │
│  │  ├─[PaddleOCR]   → 文字提取 (中文优化, 支持竖排/印章/表格)                  │   │
│  │  ├─[版面分析]    → Layout Detection (标题/正文/表格/图片区域)                │   │
│  │  ├─[表格识别]    → 结构化表格提取 (行/列/单元格)                             │   │
│  │  ├─[CLIP/ViT]    → 图像分类/标签 (证据类型/场景)                             │   │
│  │  └─后处理: 文本+表格 → Search Adapter + Milvus; 缩略图 → MinIO + CDN        │   │
│  │  性能: 单页OCR < 2s; 100页扫描PDF < 3 分钟                                  │   │
│  └────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  ┌─ 视频管道 (Video Pipeline) ──────────────────────────────────────────────┐   │
│  │  @video_queue (GPU Node, 1-2 pods)                                        │   │
│  │  原始视频(.mp4/.avi/.mov)                                                  │   │
│  │  ├─[FFmpeg]      → 关键帧提取 (场景变化检测, 1fps采样)                      │   │
│  │  ├─[PaddleOCR]   → 关键帧内文字提取                                        │   │
│  │  ├─[YOLO/CLIP]   → 场景分类/物体检测                                       │   │
│  │  ├─[Whisper]     → 视频内音频轨道转录                                       │   │
│  │  ├─[FFmpeg]      → 480p代理生成 + HLS分片                                   │   │
│  │  └─后处理: 帧序列+OCR文本 → ES; HLS流 → MinIO; 元数据 → PG                  │   │
│  │  性能: 10分钟视频关键帧 < 30s; 完整分析 < 3 分钟                             │   │
│  └────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  ┌─ 文档管道 (Document Pipeline) ────────────────────────────────────────────┐   │
│  │  @doc_queue (CPU Node, 2-5 pods)                                           │   │
│  │  原始文档(.pdf/.docx/.xlsx/.pptx/.txt)                                     │   │
│  │  ├─[unstructured.io] → PDF/Word/PPT 通用解析 (文本+表格+层次结构)           │   │
│  │  ├─[LlamaParse] (备选) → 复杂PDF(多栏/扫描件)高精度解析                      │   │
│  │  ├─[Chunking] → 智能分块 (语义边界, 512-2048 tokens/chunk)                 │   │
│  │  ├─[Embedding] → text-embedding-3-large → 1536d 向量                        │   │
│  │  └─后处理: 结构化文本 → ES/OpenSearch; 向量 → Milvus; 缩略图 → MinIO + CDN  │   │
│  │  性能: 100页PDF < 2分钟(电子原生); 扫描件含OCR < 5 分钟                      │   │
│  └────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  ┌─ 统一输出 ────────────────────────────────────────────────────────────────┐   │
│  │  所有处理结果汇总到: Elasticsearch/OpenSearch(全文检索) + Milvus(语义检索)  │   │
│  │  + PostgreSQL(结构化元数据) + MinIO(文件+缩略图, CDN分发)                    │   │
│  └────────────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────────┘
```

---

## 四、模块分解

### 4.1 模块全景

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Hermes 公共基础设施 (Common Infrastructure)       │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┬──────────┐ │
│  │JWT/RBAC  │审计日志   │知识库引擎 │文档生成   │通知服务   │配置管理   │ │
│  │三级角色   │不可篡改   │Search+Milvus│docx/xlsx│Elink/WS  │K8s CM/   │ │
│  └──────────┴──────────┴──────────┴──────────┴──────────┴──────────┘ │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┬──────────┐ │
│  │多模态处理 │全文搜索   │数据分层   │消息队列   │CDN/缓存   │监控告警   │ │
│  │4管道处理  │ES集群    │热温冷     │RabbitMQ  │Nginx+Redis│Prom+Jaeger│ │
│  └──────────┴──────────┴──────────┴──────────┴──────────┴──────────┘ │
├─────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                    │
│  │ 模块一        │  │ 模块二        │  │ 模块三        │                    │
│  │ 廉洁监察       │  │ 风险监控       │  │ 内控评价       │                    │
│  │ (6 阶段工作流) │  │ (6 阶段工作流) │  │ (13 步骤工作流)│                    │
│  │ ✅ 已实现      │  │ ⏳ 规划中      │  │ ⏳ 规划中      │                    │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘                    │
│         │                │                │                            │
│  ┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴──────┐                    │
│  │ 模块四        │  │ 模块五        │  │ 模块六        │                    │
│  │ 专项审计       │  │ 离任审计       │  │ 商业秘密       │                    │
│  │ (5 阶段工作流) │  │ (6 阶段工作流) │  │ (3 大功能)    │                    │
│  │ ⏳ 规划中      │  │ ⏳ 规划中      │  │ ⏳ 规划中      │                    │
│  └─────────────┘  └─────────────┘  └──────┬───────┘                    │
│                                            │                           │
│  ┌────────────────────────────────────────┐│                          │
│  │              模块七：行为风险              ││                          │
│  │  跨系统行为数据分析 → 异常识别 → 报告输出    ││                          │
│  │  (2 大功能)                              ││                          │
│  │  ⏳ 规划中                               ││                          │
│  └────────────────────────────────────────┘│                          │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                      模块八：持续改善                              │  │
│  │  整改承接 → 任务下发 → 进度跟踪 → AI 复核 → 闭环归档              │  │
│  │  接收所有上游模块的问题清单，统一跟踪处置闭环                       │  │
│  │  ⏳ 规划中                                                       │  │
│  └────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2 模块一：廉洁监察（反舞弊调查）✅ 已实现

**职责**：接收双轨案件来源（系统抓取 + 人工录入），通过 6 阶段 AI 辅助工作流完成从线索初判到报案协助的全流程反舞弊调查。

**工作流阶段**：

```
案件录入 → [4.1] 材料初判+分流 → [4.2] 调查方案 → [4.3] 多维分析+报告
                                        ↓
         [4.6] 报案协助 ← [4.5] 处罚执行 ← [4.4] 处置分流+追责
```

**状态定义** (`HermesState`)：包含案件基础信息（task_id, fraud_source, client）、工作流控制（current_stage, stage_history）、分流决策（should_investigate, is_hr_related）、各阶段 AI 输出（intake_report, case_conclusion, penalty_opinion）、碳基守门状态（pending_approval_stage, approval_result）、外部系统状态（risk_control_sync_status, a2a_task_ids）。

**外部交互**：
- 风控系统：字段映射同步（fraudSource ↔ reportedStaffName 等双向映射），WebSocket 状态推送
- A2A 龟宝：HR 案件转交、员工处罚跟踪回传
- A2A 西塞罗：民事纠纷推送、处罚协议审核
- A2A 波特：供应商扣款跟踪
- OA 系统：添可事业部处罚公告审批推送
- MDM：涉案供应商黑名单维护

**多模态数据**：

| 数据类型 | 来源 | 处理方式 | 预估量/案件 | 存储层 |
|----------|------|----------|------------|--------|
| 音频 (访谈录音) | 调查访谈 | Whisper ASR → 文本 → 全文索引 + Milvus | 30-120min (300-1200MB) | 热层(活跃)→温层(归档) |
| 图像 (证据截图) | 交易记录/聊天截图 | PaddleOCR → 文本 + CLIP 分类 | 20-50 张 (50-200MB) | 热层→温层 |
| 文档 (合同/协议) | 供应商合同/采购单 | unstructured.io 解析 → Chunk → Embedding → Milvus | 5-20 份 (10-100MB) | 热层→温层 |
| 视频 (监控/拜访) | 现场取证/安防监控 | 关键帧 + OCR + 音频转录 | 1-3 段 (500MB-5GB) | 温层(代理视频热层) |

### 4.3 模块二：风险监控（主动风险扫描）⏳ 规划中

**职责**：实现 7×24 小时无人值守自动风险监控，从规则生成到异常数据捕获、分析定性、自动推送的全链路主动扫描。

**工作流阶段**：

```
[6.1] 风险规则清单生成 → [6.2] 异常数据生成 → [6.3] 风险分析与主体合并
                                                   ↓
[6.6] 处置回流与指标迭代 ← [6.5] 风险结果推送 ← [6.4] 风险自动定性
```

**核心能力**：
- AI 自动生成风险场景 → 计算规则 → SQL 语句，测试环境验证后可入库
- 双重校验机制：AI 初核（剔除正常业务）+ 人工二次复核
- 按分析主体自动合并重复预警，统一口径
- 自动判定风险类型（合规风险/舞弊风险/商业秘密风险）、等级、处置建议
- 处置结果回流至知识库，持续优化规则准确率

**外部依赖**：业务数据库（SQL 查询）、企查查工商信息、舆情信息、外部法律法规

**多模态数据**：

| 数据类型 | 来源 | 处理方式 | 预估量/天 | 存储层 |
|----------|------|----------|----------|--------|
| 结构数据 (DB扫描) | 业务系统SQL查询结果 | 直接入库分析 | 1-5GB 结果集 | 热层 |
| 图像 (证据附件) | 异常推送附带截图 | OCR + 分类 | 10-50 张 (20-100MB) | 热层→温层 |
| 文档 (外部报告) | 企查查报告/舆情报告 | 文档解析 + 索引 | 5-10 份 (5-20MB) | 温层 |

### 4.4 模块三：内控评价（合规评价）⏳ 规划中

**职责**：覆盖 19 个业务循环的内控设计与执行有效性评价，自动生成审计方案、访谈问卷、测试底稿、缺陷清单和评价报告。

**工作流阶段**：

```
立项 → 审计方案生成(5部分) → 访谈清单/计划 → 访谈问卷 → 完善风控矩阵
                                                              ↓
出具报告 ← 内控评价总体打分 ← 缺陷确认 ← 执行缺陷评估 ← 设计缺陷评估
                                  ↑                          ↑
                           资料需求清单 ←────────────────────┘
```

**特殊依赖**：
- 内控评价模块维护的"制度库"需向商业秘密模块提供制度比对能力
- 对接西塞罗 Agent 的岗位职责功能，用于访谈人员匹配

**多模态数据**：

| 数据类型 | 来源 | 处理方式 | 预估量/项目 | 存储层 |
|----------|------|----------|------------|--------|
| 音频 (访谈录音) | 内控访谈 | Whisper ASR → 文本 | 5-15 段 (500MB-3GB) | 热层→温层 |
| 文档 (制度文件) | 公司制度/流程文档 | 文档解析 → 制度库索引 | 50-200 份 (50-200MB) | 热层(活跃) |
| 图像 (证据截图) | 穿行测试截图 | OCR + 分类 | 30-100 张 (50-200MB) | 热层→温层 |
| 视频 (操作录像) | 系统操作录屏 | 关键帧 + OCR | 2-5 段 (200MB-2GB) | 温层 |

### 4.5 模块四：专项审计（专项检查）⏳ 规划中

**职责**：基于审计目的和重点，自动生成标准化审计方案、访谈问卷、资料检查计划，执行检查作业并输出审计报告。

**工作流阶段**：

```
审计方案生成 → 访谈作业 → 检查作业 → 问题确认 → 出具审计报告
```

**核心能力**：
- 审计方案自动匹配：检索与审计目的/重点关联的历史方案，综合输出优化方案
- 抽样策略 AI 建议：根据业务量级和风险程度建议样本量和抽样方法
- 多数据源分析：SQL 直查数据中台 + API 获取外部数据 + 人工上传窗口
- 问题逐条下发确认，AI 分析反馈合理性

**多模态数据**：

| 数据类型 | 来源 | 处理方式 | 预估量/项目 | 存储层 |
|----------|------|----------|------------|--------|
| 音频 (审计访谈) | 审计对象访谈 | Whisper ASR → 文本 | 3-10 段 (300MB-2GB) | 热层→温层 |
| 文档 (审计资料) | 被审计单位提供 | 文档解析 + 索引 | 30-100 份 (30-100MB) | 热层→温层 |
| 图像 (检查证据) | 现场检查拍照 | OCR + 分类 | 20-50 张 (30-100MB) | 热层→温层 |

### 4.6 模块五：离任审计（离职审查）⏳ 规划中

**职责**：根据被审计人职责配置和任职时间，自动生成离任审计方案，识别个人问题和业务问题。

**工作流阶段**：

```
审计方案生成 → 访谈问卷 → 资料清单 → 问题清单生成 → 问题确认 → 出具报告
```

**特殊匹配逻辑**：
- 读取被审计人岗位职责 → 在业务审计方案库中检索强关联内容 → 在个人审计方案库中检索通用检查项
- 审计期间自动计算：1 年<本岗位≤5 年 → 向前计算三年；>5 年 → 向前计算一年
- 问题清单二分：个人问题（商业秘密泄露、个人报销、样机使用、关联公司）+ 业务问题（流程漏洞、制度缺陷、经济损失）
- 接收其他模块（如风险监控、行为风险）传回的行为风险预警结果

**多模态数据**：

| 数据类型 | 来源 | 处理方式 | 预估量/人 | 存储层 |
|----------|------|----------|----------|--------|
| 文档 (个人档案) | HR 系统导出 | 文档解析 + 索引 | 5-20 份 (5-20MB) | 热层 |
| 图像 (报销凭证) | 财务系统截图 | PaddleOCR + 表格提取 | 10-30 张 (20-50MB) | 热层→温层 |
| 文档 (离职材料) | 离职交接单/审计底稿 | 文档解析 | 3-10 份 (3-10MB) | 热层→温层 |

### 4.7 模块六：商业秘密保护（保密管理）⏳ 规划中

**职责**：提供保密员定密预审建议和集团评审小组定密信息评审，辅助定密流程的专业性和完整性。

**三大功能**：

| 功能 | 说明 | 多模态数据 | 存储层 |
|------|------|-----------|--------|
| **保密员定密建议/预审** | 保密员提交定密信息前的 AI 预审，输出预审报告（含前期已定密信息评审结果 + 建议定密内容），格式与《商业秘密信息表》模板一致，可直接导入系统 | 文档(定密信息表、制度文件)/图像(定密物照片) | 热层 |
| **定密信息评审** | 评审小组组织的正式评审，AI 综合分析内部制度、外部法规、侵权案例、同类部门横向比对，出具评审报告 | 文档(法规库、案例库、历史评审记录) | 热层(活跃评审)/温层(历史) |
| **管理报告** | 月度定期生成，含整体进度/数量统计，支持全集团/事业部/部门维度切换 | 文档(自动生成报告) | 温层 |

**特殊依赖**：需等待内控评价智能体接入制度文件后，调用制度库进行比对；行为风险模块分析结果可作为定密评审的辅助数据源。

### 4.8 模块七：行为风险（员工行为分析）⏳ 规划中

**职责**：对各监管系统产生的行为数据进行跨系统综合分析，识别异常的员工行为，实现跨系统数据整合与智能分析，提升行为风险识别的效率和准确性。

**两大功能**：

| 功能 | 说明 | 多模态数据 | 存储层 |
|------|------|-----------|--------|
| **行为风险分析** | 选择范围（事业部/部门/岗位/人员/角色/期间），输出行为风险分析报告，包括异常行为识别、风险等级评估、关联分析结论等；支持查看往期分析报告 | 文档(分析报告) | 热层(活跃)/温层(历史) |
| **管理情况报告** | 月度定期生成，内容包括：监管系统覆盖范围、缺失范围、行为数据质量评估、高风险行为汇总、高频行为问题统计、优化方案和建议；支持查看往期管理报告 | 文档(管理报告) | 温层 |

**数据来源与知识库**：
- 各监管系统行为数据（核心分析数据源，需与 IT 沟通数据对接）
- 风控系统：员工生命周期信息、利益申报信息、商业秘密信息、涉密文件清单
- HR 系统/德鲁克：员工档案信息（邮箱、微信/QQ、手机号等）、岗位职责
- MDM/德鲁克：员工部门架构信息
- 威科先行：外部法律法规、侵权诉讼案件信息
- 前期行为分析结果（历史分析结论回溯与趋势研判）

**外部交互**：
- 行为风险分析结果作为离任审计问题清单生成的数据源之一（预警 → 问题清单）
- 风险监控发现的员工行为异常可推送至行为风险模块进行专项深度分析（双向联动）
- 可接收风险监控模块的行为风险预警结果

**多模态数据**：

| 数据类型 | 来源 | 处理方式 | 预估量/周期 | 存储层 |
|----------|------|----------|------------|--------|
| 结构数据 (行为日志) | 各监管系统行为数据 | 直接入库分析 | 1-5GB/月 | 热层 |
| 文档 (外部法规/案例) | 威科先行/风控系统 | 文档解析 + 索引 | 10-50 份 (5-50MB) | 热层→温层 |
| 文档 (分析报告) | AI 自动生成 | 报告模板填充 → MinIO | 5-20 份 (5-20MB)/月 | 热层(当月)→温层(历史) |

### 4.9 模块八：持续改善（整改跟踪）⏳ 规划中

**职责**：统一承接所有上游模块（廉洁监察、内控评价、专项审计、离任审计、行为风险）的审计问题清单，实现从录入、下发、整改、复核到归档的全流程闭环跟踪。是七大执行模块的数据汇合与整改统一出口。

**核心子模块**：

```
┌────────────────────────────────────────────────────────────┐
│                     持续改善智能体                          │
├───────────┬───────────┬───────────┬───────────┬───────────┤
│ 问题录入   │ 任务下发   │ 整改跟踪   │ 复核闭环   │ 问题归档   │
│ 30+字段    │ 一键下发   │ 筛选查看   │ AI 初审    │ 自动归档   │
│ 批量导入   │ AI 初审    │ 一键催办   │ 二次复核   │ 条件导出   │
│ 校验规则   │ 计划审批   │ 逾期提醒   │ 退回重改   │ 不可编辑   │
├───────────┴───────────┴───────────┴───────────┴───────────┤
│  统计分析：整改完成率、逾期率、部门效率可视化（柱状图/饼图/折线图）│
│  操作日志：全流程留痕，不可删除不可篡改                        │
└────────────────────────────────────────────────────────────┘
```

**状态流转**：问题待推送 → 计划待提交 → 计划待审批 → 整改答复待提交 → 已整改待复核 → 整改完成

**权限模型**：项目组长（全项目可见）→ 项目成员（分配可见）→ 事业部负责人（事业部可见）→ 风控负责人（全量可见）

**多模态数据**：

| 数据类型 | 来源 | 处理方式 | 预估量/问题 | 存储层 |
|----------|------|----------|------------|--------|
| 文档 (整改证据) | 整改责任人上传 | OCR + 文档解析 → ES索引 | 2-10 份 (2-20MB) | 热层(未复核)→温层(已闭环) |
| 图像 (整改前后对比) | 现场拍照 | OCR + 图像对比 | 2-5 张 (5-20MB) | 热层→温层 |

### 4.10 模块数据量估算（10TB 总体规模）

| 模块 | 文档/附件 | 音频 | 图像 | 视频 | 结构化数据 | 合计估算 |
|------|----------|------|------|------|-----------|---------|
| 廉洁监察 | 200GB | 1.5TB | 300GB | 2TB | 50GB | ~4TB |
| 风险监控 | 100GB | — | 50GB | — | 500GB(扫描结果) | ~650GB |
| 内控评价 | 300GB | 800GB | 200GB | 500GB | 30GB | ~1.8TB |
| 专项审计 | 200GB | 500GB | 100GB | — | 20GB | ~820GB |
| 离任审计 | 100GB | — | 50GB | — | 10GB | ~160GB |
| 商业秘密 | 500GB(法规/案例) | — | 50GB | — | 10GB | ~560GB |
| 行为风险 | 50GB(报告/法规) | — | — | — | 100GB(行为日志) | ~150GB |
| 持续改善 | 100GB | — | 50GB | — | 20GB | ~170GB |
| 共享基础(ES索引/KB/日志) | — | — | — | — | — | ~1.8TB |
| **合计** | **~1.55TB** | **~2.8TB** | **~800GB** | **~2.5TB** | **~740GB** | **~10TB** |

### 4.11 共享基础设施

| 基础设施 | 说明 | 使用方 |
|----------|------|--------|
| **统一认证 (JWT + RBAC)** | 三级角色（集团/科沃斯/添可），行级数据过滤，所有 API 路由自动注入；Token 黑名单 Redis Cluster 全局同步 | 全部模块 |
| **审计日志** | 全量操作记录（操作人、时间、IP、内容、结果），不可删除篡改；ES 索引保留 1 年可搜索，归档保留 10 年 | 全部模块 |
| **知识库引擎 (RAG)** | RAG Orchestrator 统一编排 Elasticsearch/OpenSearch 全文召回 + Milvus 向量召回 + metadata filter + rerank + 引用追溯 | 全部模块 |
| **多模态处理服务** | 音频/图像/视频/文档 四管道异步处理，文本提取 + 结构化 + 索引 | 全部模块 |
| **全文搜索 (Search Adapter)** | 跨案件/文档/证据/聊天记录统一全文检索；生产默认 Elasticsearch 9.x，许可或开源策略限制时切换 OpenSearch | 全部模块 |
| **向量检索 (Milvus)** | 知识库、证据、历史案例向量召回；集合按模块和租户隔离，PostgreSQL 仅保存知识元数据和索引版本 | 全部模块 |
| **文档生成服务** | Python-docx + OpenPyXL 模板填充，支持 Word/Excel 批量导出，Celery 异步执行 | 全部模块 |
| **通知服务** | Elink 消息通知 + WebSocket 实时推送 + 任务中心消息分发 | 全部模块 |
| **配置管理** | Pydantic Settings，.env 环境注入 / K8s ConfigMap + Secret，支持 LLM/DB/Redis/MinIO/JWT 等全局配置 | 全部模块 |
| **数据分层迁移** | 自动化生命周期管理：热→温(案件关闭 90 天)、温→冷(案件关闭 2 年)、冷保留 10 年；可搜索元数据索引 | 全部模块 |

---

## 五、技术选型

### 5.1 整体技术栈

| 层次 | 技术选型 | 版本 | 选型理由 |
|------|----------|------|----------|
| **后端框架** | FastAPI (Python 3.11+) | ≥0.115 | 原生异步支持，OpenAPI 自动生成，生态成熟 |
| **工作流引擎** | LangGraph | 当前稳定版 | 原生 durable checkpointer + interrupt/resume，作为唯一 Workflow Runtime |
| **LLM 网关** | Model Gateway + Provider Adapter | — | 统一 DeepSeek、Qwen、私有模型 endpoint；支持路由、熔断、灰度、成本与质量评估 |
| **Web 前端** | Vue 3 + TypeScript | 3.x | 组合式 API，Element Plus 企业级组件库 |
| **构建工具** | Vite (前端) / uv (后端) | — | Vite 冷启动快；uv 为 Rust 实现的 Python 包管理器，比 pip 快 10-100x |
| **状态管理** | Pinia | — | Vue 3 官方推荐，TypeScript 友好 |
| **富文本编辑器** | Tiptap (Vue 适配) | — | 基于 ProseMirror，支持划词调整、方案变更 |
| **UI 组件库** | Element Plus | 2.x | 成熟的企业级 Vue 3 组件库 |
| **PDF 预览** | PDF.js (Vue 适配) | — | 浏览器端分页渲染，支持 1000+ 页文档流式加载 |
| **视频播放** | HLS.js + DPlayer | — | HLS 自适应码率分段传输，支持视频关键帧时间轴标注 |
| **音频播放** | Wavesurfer.js | — | 音频波形可视化 + 时间轴标注转写文本对齐 |

### 5.2 数据与存储

| 组件 | 选型 | 版本 | 用途 | 选型理由 |
|------|------|------|------|----------|
| **关系数据库** | PostgreSQL | 18 当前稳定版；DBA/插件未就绪时退到 17/16 当前小版本 | 业务事实、审批、任务、Outbox/Inbox、审计日志 | 成熟稳定，支持 JSONB、分区表、RLS；作为业务事实库而非大规模 AI 检索主库 |
| **轻量向量扩展** | pgvector | 0.7+ | 小规模模块知识库、开发/测试、局部相似检索 | 支持 HNSW/IVFFlat；生产 10TB 级知识检索不作为主引擎 |
| **向量检索服务** | Milvus Distributed | 当前稳定版 | 知识库、证据、历史案例的大规模向量召回 | 与业务 OLTP 物理隔离，支持大规模向量集合、水平扩展和独立容量治理 |
| **ORM** | SQLAlchemy 2.0 (async) | ≥2.0 | 数据库映射与查询 | asyncio 原生支持，与 FastAPI 深度集成 |
| **数据库迁移** | Alembic | ≥1.14 | Schema 版本管理 | SQLAlchemy 官方迁移工具 |
| **连接池** | PgBouncer | ≥1.22 | 数据库连接池 | 生产峰值 500 并发需要 pool_size=500 transaction 模式连接池 |
| **缓存/会话** | Redis Cluster | 7 | Session、限流计数、热点缓存、checkpoint 热缓存 | 3 主 3 从集群；不作为关键 workflow state 唯一持久化 |
| **对象存储 (温层)** | MinIO 集群 | RELEASE.2025+ | 附件/文档/音视频/缩略图 | S3 兼容 API，4 节点 EC 12+4，本地部署 |
| **冷层归档** | NAS / 磁带库 | — | 历史案件归档 (> 2 年) | 低成本长期存储，法规要求保留 10 年 |
| **全文搜索** | Elasticsearch 9.x / OpenSearch | 当前稳定版 | 10TB 文档全文检索、审计日志搜索、证据检索 | 通过 Search Adapter 隔离产品差异；许可或开源策略变化时可切换 |
| **负载均衡** | Nginx + Keepalived | 1.26+ | 反向代理、SSL 终结、CDN 缓存、API 负载均衡 | least_conn 算法、sticky session、健康检查 |

### 5.3 消息队列与异步处理

| 组件 | 选型 | 版本 | 用途 | 选型理由 |
|------|------|------|------|----------|
| **消息队列** | RabbitMQ | 3.13+ | 核心消息总线，9 个业务 quorum queues + DLX 死信队列 | 消息持久化、ACK 确认、quorum queues 高可用复制、优先级/延迟投递、联邦 |
| **任务分发** | Celery | ≥5.4 | Worker 任务执行框架 | 成熟的任务队列（Kombu 连接 RabbitMQ），支持重试、优先级、任务链 |
| **任务监控** | Flower | ≥2.0 | Celery 任务监控 Dashboard | 实时查看任务状态、成功率、队列深度 |

> **架构边界说明**：RabbitMQ 负责消息可靠投递，Celery 负责任务执行，LangGraph 负责工作流语义。Worker 完成任务后只写任务结果并发布事件，由 Workflow Runtime 消费事件恢复节点；Redis Cluster 仅用于缓存、会话、限流和 checkpoint 热缓存。

### 5.4 AI/ML 组件

| 组件 | 选型 | 用途 | 选型理由 |
|------|------|------|----------|
| **模型网关** | Model Gateway + Model Registry | 模型路由、熔断、灰度、成本和质量治理 | 不在业务代码中硬编码模型版本，支持多 provider 策略 |
| **主模型 Provider** | DeepSeek | 对话生成、推理、报告撰写 | 中文能力优秀，API 兼容 OpenAI 格式，推理成本低 |
| **备用模型 Provider** | Qwen / 通义千问 | LLM 降级备份 | 阿里云生态，中文理解力强，OpenAI-compatible 接入 |
| **私有模型 Provider** | 本地大模型 endpoint | 高敏场景、成本控制、容灾补充 | 通过 provider adapter 接入，接受统一评测和路由策略 |
| **Embedding 模型** | text-embedding-3-large (1536d) | 知识库文档向量化 | OpenAI 兼容接口，中文语义捕获好 |
| **语音转文字** | Whisper large-v3 (自部署) | 音频转文字、访谈记录转录 | 开源、多语言、GPU 加速、批量离线处理；Tool 模式解耦 |
| **说话人分离** | pyannote.audio (自部署) | 音频说话人识别与分离 | 开源、可与 Whisper 集成，支持多人对话场景 |
| **OCR** | PaddleOCR (自部署) | 中文文档/图像文字提取 | 中文准确率 > Tesseract，支持表格/印章/版面分析，GPU 加速 |
| **图像分类** | CLIP / ViT (自部署) | 证据图像自动分类 | 零样本分类能力，支持中文标签；可针对业务场景微调 |
| **文档解析** | unstructured.io | PDF/Word/Excel/PPT 通用解析 | 开源，支持多格式、元素分类、Chunking；纯内网部署 |
| **文档解析备选** | LlamaParse | 复杂 PDF (多栏/扫描件) 高精度解析 | 高精度，支持 Markdown 输出，保留阅读顺序 |
| **LLM 可观测** | LangFuse | Token 消耗、延迟、Tool 调用追踪 | 开源，LangChain 原生集成 |

### 5.5 可观测性

| 组件 | 选型 | 用途 |
|------|------|------|
| **LLM 追踪** | LangFuse | Agent 节点 Token 消耗、LLM 延迟 P50/P95/P99、Tool 成功率、KB 检索命中率、Prompt 版本追踪 |
| **统一遥测标准** | OpenTelemetry | trace_id 贯穿 API、Workflow、Worker、LLM、RAG、Tool、外部系统 |
| **系统监控** | Prometheus + Grafana | API QPS/P95 延迟/错误率、PG 连接池、Redis 命中率、RabbitMQ 深度、Celery 统计、K8s Pod/GPU 资源、搜索与向量库延迟 |
| **业务日志** | structlog | 结构化 JSON 日志，traceId 链路追踪，敏感字段自动脱敏；ES 索引 1 年 |
| **分布式追踪** | Jaeger / Tempo | OpenTelemetry trace backend，支持跨服务调用链追踪、瓶颈分析和服务依赖图 |
| **业务仪表板** | Grafana Business Dashboard | 10K 注册用户活跃度、案件处理量/趋势、存储使用趋势 (热/温/冷)、多模态处理吞吐量 |

### 5.6 Python 依赖

| 类别 | 核心包 | 版本 |
|------|--------|------|
| Web | fastapi, uvicorn[standard], python-multipart | ≥0.115 |
| LangGraph | langgraph, langchain, langchain-openai, langchain-community | ≥0.2 / ≥0.3 |
| 数据库 | sqlalchemy[asyncio], asyncpg, alembic, pgvector, psycopg2 (PgBouncer) | ≥2.0 |
| 缓存/队列 | redis[hiredis], celery, flower | ≥5.2 |
| 消息队列 | kombu (RabbitMQ client for Celery) | ≥5.4 |
| 搜索/向量 | elasticsearch[async] 或 opensearch-py, pymilvus | 当前稳定版 |
| 数据验证 | pydantic, pydantic-settings | ≥2.10 |
| 认证 | python-jose[cryptography], passlib[bcrypt] | ≥3.3 |
| 文档生成 | python-docx, openpyxl | ≥1.1 |
| HTTP | httpx | ≥0.28 |
| 可观测性 | structlog, langfuse, opentelemetry-api, opentelemetry-sdk, opentelemetry-instrumentation-fastapi, opentelemetry-instrumentation-celery, OTLP exporter | 当前稳定版 |
| 文件存储 | minio | ≥7.2 |
| 多模态 | openai-whisper, faster-whisper, paddleocr, paddlepaddle-gpu, opencv-python-headless, unstructured[pdf,docx,xlsx], pillow | — |

### 5.7 基础设施版本矩阵

| 组件 | 版本 | 部署模式 |
|------|------|----------|
| K8s | 1.30+ | 3 Master + 5 Worker + 2 GPU Worker |
| PostgreSQL | 18 当前稳定版；DBA/插件未就绪时 17/16 当前小版本 | CloudNativePG 或 Patroni + Primary + 2 Read Replicas + PgBouncer |
| Redis | 7 | 3 Master + 3 Slave Cluster |
| MinIO | RELEASE.2025+ | 4 节点, EC 12+4, ~20TB 可用 |
| Elasticsearch / OpenSearch | Elasticsearch 9.x 或 OpenSearch 当前稳定版 | 3+ 节点, 1 副本, 分片按索引容量规划 |
| Milvus | 当前稳定版 | Distributed + 独立对象存储/消息组件，集合按模块和租户隔离 |
| RabbitMQ | 3.13+ | 3 节点 quorum queues 集群 |
| Nginx | 1.26+ (alpine) | 2 节点 + Keepalived VIP |
| Jaeger / Tempo | 当前稳定版 | OpenTelemetry trace backend |
| Docker Registry | Harbor | 内网镜像仓库 (等保要求) |

---



## 六、部署架构

### 6.1 Kubernetes 集群拓扑

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│                         企业内部网络 (科沃斯内网)                                    │
│                                                                                  │
│  ┌─────────────────────── DMZ / 接入区 ──────────────────────────────────────┐   │
│  │  ┌─────────────────────────────────────────────────────────────────────┐  │   │
│  │  │              负载均衡层 (Load Balancer Layer)                          │  │   │
│  │  │  ┌──────────────────────┐        ┌──────────────────────┐            │  │   │
│  │  │  │  Nginx Node 1        │◄──────►│  Nginx Node 2        │            │  │   │
│  │  │  │  IP: 10.x.x.11       │Keepalive│  IP: 10.x.x.12       │            │  │   │
│  │  │  │                      │  VIP   │                      │            │  │   │
│  │  │  │  SSL 终结 (:443)     │10.x.x.10│  SSL 终结 (:443)     │            │  │   │
│  │  │  │  静态资源 + CDN       │        │  静态资源 + CDN       │            │  │   │
│  │  │  │  API 反向代理        │        │  API 反向代理        │            │  │   │
│  │  │  │  WebSocket 代理      │        │  WebSocket 代理      │            │  │   │
│  │  │  │  Rate Limiting       │        │  Rate Limiting       │            │  │   │
│  │  │  └──────────────────────┘        └──────────────────────┘            │  │   │
│  │  └─────────────────────────────────────────────────────────────────────┘  │   │
│  └────────────────────────────────────────────────────────────────────────────┘   │
│                                        │                                           │
│  ┌─────────────────────── K8s 集群 (应用区) ────────────────────────────────────┐   │
│  │                                                                              │   │
│  │  Master 节点 (3台, 4C/8G/100GB SSD): etcd + api-server + controller + scheduler│
│  │                                                                              │   │
│  │  ┌─────────── 通用工作节点 (3台, CPU密集型) ──────────────────────────────┐  │   │
│  │  │  k8s-worker-01 (16C/32G/200GB SSD)                                     │  │   │
│  │  │  ├── api-deployment (3-10 pods, FastAPI ASGI)                           │  │   │
│  │  │  ├── workflow-runtime (3-10 pods, LangGraph durable runtime)             │  │   │
│  │  │  ├── celery-report-worker (2-4 pods)  ├── celery-sync-worker (1-3)     │  │   │
│  │  │  ├── celery-a2a-worker (1-3 pods)     └── celery-kb-worker (1-3)       │  │   │
│  │  │                                                                         │  │   │
│  │  │  k8s-worker-02 (16C/32G/200GB SSD)                                     │  │   │
│  │  │  ├── api-deployment (3-10 pods)      ├── celery-doc-worker (2-5)       │  │   │
│  │  │  ├── celery-report-worker (2-4)      └── celery-llm-worker (1-5)       │  │   │
│  │  │                                                                         │  │   │
│  │  │  k8s-worker-03 (16C/32G/200GB SSD)                                     │  │   │
│  │  │  ├── api-deployment (2-5 pods, 溢出)  ├── celery-doc-worker (1-3)     │  │   │
│  │  │  ├── celery-llm-worker (1-3)           └── 可观测性 Pods               │  │   │
│  │  └─────────────────────────────────────────────────────────────────────────┘  │   │
│  │                                                                              │   │
│  │  ┌─────────── GPU 工作节点 (2台, 多模态处理) ──────────────────────────┐   │   │
│  │  │  k8s-gpu-worker-01 (16C/64G/200GB SSD + NVIDIA V100 32G)             │  │   │
│  │  │  ├── celery-audio-worker (1-3 pods, Whisper+pyannote)                 │  │   │
│  │  │  ├── celery-image-worker (1-3 pods, PaddleOCR+CLIP)                   │  │   │
│  │  │  └── celery-video-worker (1-2 pods, OpenCV+FFmpeg)                    │  │   │
│  │  │                                                                       │  │   │
│  │  │  k8s-gpu-worker-02 (16C/64G/200GB SSD + NVIDIA V100 32G)             │  │   │
│  │  │  ├── celery-audio-worker (1-2, 溢出)  ├── celery-image-worker (1-2)  │  │   │
│  │  │  └── celery-video-worker (0-1, 溢出)                                  │  │   │
│  │  └───────────────────────────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  ┌─────────────────────── 数据存储区 ──────────────────────────────────────────┐   │
│  │                                                                              │   │
│  │  PostgreSQL 业务事实库: Primary(8C/32G/2TB SSD) + 2 Read Replicas(8C/32G)   │   │
│  │  + PgBouncer(2C/4G, pool_size=500) → 案件/审批/审计/Outbox/Inbox/checkpoint  │   │
│  │                                                                              │   │
│  │  Redis 7 Cluster: 3 Master + 3 Slave → Session/Cache/RateLimit/热点状态缓存  │   │
│  │                                                                              │   │
│  │  MinIO 4 节点集群: EC 12+4, 各 4TB HDD, ~20TB 可用 → 温层                    │   │
│  │                                                                              │   │
│  │  Elasticsearch 9.x / OpenSearch: 各 8C/32G/1TB SSD → 全文检索/Search Adapter│   │
│  │                                                                              │   │
│  │  Milvus Distributed: 向量召回，集合按模块/租户隔离 → 知识库与证据向量检索      │   │
│  │                                                                              │   │
│  │  RabbitMQ 3 节点: 各 4C/16G/200GB SSD, quorum queues → 消息总线            │   │
│  │                                                                              │   │
│  │  NAS / 磁带归档: 冷层历史数据, 保留 10 年 (法规要求)                           │   │
│  │                                                                              │   │
│  │  监控: Prometheus + Grafana + Jaeger + LangFuse + Harbor                     │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  ┌─────────────────────── 外部依赖区 ──────────────────────────────────────────┐   │
│  │  风控系统 │ OA(BPM) │ MDM │ Model Gateway Providers │ 私有模型 endpoint │   │
│  │  龟宝(A2A) │ 西塞罗(A2A) │ 波特(A2A)                                       │   │
│  └──────────────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 服务清单与资源规划

| K8s Deployment | 副本 (min/max) | CPU Req/Limit | Memory Req/Limit | GPU | HPA 策略 |
|------|------|------|------|------|------|
| **api** | 3/20 | 1/2 core | 2G/4G | — | CPU > 70% 或 P95 > 300ms |
| **workflow-runtime** | 3/20 | 1/2 core | 2G/4G | — | 队列/事件积压、运行中 workflow 数、P95 恢复耗时 |
| **celery-audio-worker** | 1/3 | 2/4 | 8G/16G | V100 x1 | 队列深度 > 10 |
| **celery-image-worker** | 1/3 | 2/4 | 8G/16G | V100 x1 | 队列深度 > 20 |
| **celery-video-worker** | 1/2 | 2/4 | 8G/16G | V100 x1 | 队列深度 > 5 |
| **celery-doc-worker** | 2/5 | 2/4 | 4G/8G | — | 队列深度 > 30 |
| **celery-report-worker** | 2/4 | 1/2 | 2G/4G | — | 队列深度 > 20 |
| **celery-sync-worker** | 1/3 | 1/2 | 2G/4G | — | 队列深度 > 10 |
| **celery-a2a-worker** | 1/3 | 1/2 | 2G/4G | — | 队列深度 > 10 |
| **celery-kb-worker** | 1/3 | 1/2 | 4G/8G | — | 队列深度 > 10 |
| **celery-llm-worker** | 1/5 | 0.5/2 | 2G/4G | — | 队列深度 > 50 |

### 6.3 HPA 扩缩容策略

```
  自动扩缩 (HPA - Horizontal Pod Autoscaler):
  API 服务:
  ├── 指标: CPU > 70% 持续 2min → 扩容 +2
  ├── 指标: 请求延迟 P95 > 500ms 持续 2min → 扩容 +2
  ├── 指标: CPU < 30% 持续 10min → 缩容 -1
  ├── 冷却: 扩容 1min / 缩容 5min
  └── Pod Disruption Budget: minAvailable=2

  Worker 服务 (通用):
  ├── 指标: RabbitMQ 队列深度 > 阈值 → 扩容 +1
  ├── 指标: 队列深度 < 阈值/2 持续 10min → 缩容 -1
  └── GPU Worker: GPU 资源稀缺，优先保留而非缩容

  定时扩缩 (CronHPA):
  ├── 工作日 8:00 → API 预热到 5 副本 (应对上班高峰)
  ├── 工作日 9:00-12:00 → API 保持 8-20 副本 (核心工作时段)
  ├── 工作日 20:00 → API 缩容到 3 副本 (非工作时间)
  ├── 周末/节假日 → API 保持 2 副本 (风险监控 7x24)
  └── 风险监控 Worker: 7x24 最少 1 副本，工作日峰值 3 副本
```

**K8s 运行治理基线（生产必备）**：

| 治理项 | 要求 |
|--------|------|
| Namespace/RBAC | `hermes-prod`、`hermes-staging`、`hermes-dev` 独立 Namespace；ServiceAccount 最小权限；禁止业务 Pod 使用 cluster-admin |
| ResourceQuota/LimitRange | 每个 Namespace 配置 CPU/内存/GPU 配额与默认 requests/limits，避免无界资源消耗 |
| PDB/反亲和 | API、Worker、PgBouncer、RabbitMQ、Redis、ES、MinIO 均配置 PodDisruptionBudget；关键副本跨节点反亲和或 TopologySpread |
| GPU 调度 | GPU 节点使用 taint/toleration 与 nodeSelector；仅 audio/image/video worker 可申请 GPU |
| 存储 | StatefulSet 使用明确 StorageClass 与 PVC；PG/ES/Redis/RabbitMQ/MinIO 禁止使用 emptyDir 持久化 |
| Stateful Operator | PostgreSQL 优先 CloudNativePG 或 DBA 标准 Patroni；全文检索使用 ECK/OpenSearch Operator；MinIO 使用 MinIO Operator；监控使用 Prometheus Operator |
| 探针 | API/Worker 暴露 liveness/readiness/startup probe；依赖不可用时 readiness 失败、liveness 不盲目重启 |
| 弹性指标 | API 使用 HPA（CPU/内存/延迟）；Worker 使用 KEDA 读取 RabbitMQ 队列深度；容量触发阈值见 §8.16 |
| 维护窗口 | 节点 drain 前先确认 PDB、备份状态和队列积压；生产节点升级需灰度并保留回滚窗口 |

### 6.4 网络拓扑与安全分区

```
  网络分区 (VLAN):
  VLAN 10: DMZ/接入层    → Nginx x2 (仅开放 80/443)
  VLAN 20: 应用层        → K8s Worker x5 + GPU Worker x2 (不对外暴露端口)
  VLAN 30: 数据层        → PostgreSQL/Redis/MinIO/ES或OpenSearch/Milvus/RabbitMQ/NAS
  VLAN 40: 管理层        → K8s Master/Harbor/Prometheus/Grafana/Jaeger或Tempo/堡垒机

  防火墙规则:
  ├── DMZ → 应用层: 仅 TCP 8000 (API/Ingress 上游)
  ├── 应用层 → 数据层: TCP 5432(PG), 6379(Redis), 9000(MinIO), 9200(Search), 19530(Milvus), 5672(AMQP)
  ├── 管理层 → 所有: SSH(堡垒机), Prometheus metrics, K8s API, RabbitMQ/MinIO/Search/Milvus 管理端
  ├── 外部 Webhook → DMZ: 仅 HTTPS 443，经签名校验与 IP 白名单后转发到 API
  └── 出网白名单: Model Gateway providers, 私有模型 endpoint, 企查查 API, 外部Agent回调
```

### 6.5 数据库集群配置

```
  PostgreSQL 业务事实库 (18 当前稳定版；DBA/插件未就绪时 17/16 当前小版本):
  CloudNativePG 或 Patroni 管理 Primary 自动选主与故障转移
  Primary (8C/32G/2TB) → WAL Streaming → Replica-1 (8C/32G/2TB)
                                        → Replica-2 (8C/32G/2TB)
  PgBouncer:
  ├── write pool: 路由到当前 Patroni primary
  └── read pool: 路由到健康 replica；故障时临时回退 primary
  分区策略:
  ├── cases:      按 created_at RANGE 分区 (年)
  ├── audit_logs: 按 created_at RANGE 分区 (月)
  ├── case_evidences: 按 case_id HASH 分区
  ├── workflow_checkpoints: 按 thread_id HASH 分区 + created_at RANGE 子分区
  ├── event_outbox/event_inbox: 按 created_at RANGE 分区
  └── knowledge_metadata: 按 kb_type LIST 分区，仅保存知识元数据、索引版本和对象引用

  索引策略:
  ├── B-tree: 高频查询字段 (case_id, user_id, status)
  ├── GIN: JSONB 字段和少量本地全文降级字段
  ├── 部分索引: 仅活跃状态的案件索引
  └── 向量主索引不在业务库内，交由 Milvus Distributed 承载

  备份策略:
  ├── pgBackRest 全量/增量备份 → MinIO + NAS 双副本
  ├── WAL 连续归档 → MinIO（异地副本）
  ├── PITR: 支持任意时间点恢复 (保留 30 天 WAL)
  ├── 每季度执行恢复演练并记录实际 RPO/RTO
  └── pg_basebackup 仅用于新副本初始化或人工修复，不作为生产主备份方案
```

### 6.6 部署流程

```
1. 基础环境准备
   ├── 服务器初始化 (OS安装、网络配置、防火墙规则、NTP同步)
   ├── K8s 集群搭建 (kubeadm init + join, 3 Master + 5 Worker + 2 GPU Worker)
   ├── 网络插件 (Calico)
   ├── GPU Operator (NVIDIA GPU Operator for K8s)
   └── Harbor 内网镜像仓库部署

2. 基础设施部署 (K8s StatefulSet + Service)
   ├── PostgreSQL 集群 (CloudNativePG 或 Patroni + Primary + 2 Replicas + PgBouncer + pgBackRest)
   ├── Redis Cluster (3 Master + 3 Slave)
   ├── MinIO 集群 (4 节点, EC 12+4)
   ├── Elasticsearch 9.x 或 OpenSearch 集群 (Search Adapter)
   ├── Milvus Distributed 向量检索集群
   ├── RabbitMQ 集群 (3 节点, quorum queues)
   └── Jaeger (+ ES 后端)

3. 应用部署 (K8s Deployment + HPA)
   ├── ConfigMap 创建 + External Secrets/Vault 同步敏感配置
   ├── kubectl apply -f k8s/api-deployment.yaml
   ├── kubectl apply -f k8s/worker-gpu-deployments.yaml
   ├── kubectl apply -f k8s/worker-cpu-deployments.yaml
   └── kubectl apply -f k8s/hpa-configs.yaml

4. 数据库初始化
   ├── alembic upgrade head (K8s Job)
   ├── python -m hermes.scripts.seed_kb (初始化知识库)
   ├── Elasticsearch 索引模板 (ik_max_word 分词器)
   └── RabbitMQ exchange + quorum queues + DLX + replay queue 创建

5. 监控部署
   ├── Prometheus + Grafana (K8s Prometheus Operator)
   ├── LangFuse (K8s Deployment)
   ├── K8s Dashboard + AlertManager
   └── 告警规则配置 (→ Elink/邮件)

6. 验证
   ├── 健康检查: /health + K8s liveness/readiness probe
   ├── 压力测试: Locust 模拟 500 并发用户
   ├── 故障演练: 主动 Kill Pod → 验证 HPA 自动恢复
   └── 备份验证: 执行完整数据库恢复演练
```

### 6.7 环境变量与密钥管理 (External Secrets/Vault + ConfigMap)

生产环境使用 HashiCorp Vault 或 External Secrets Operator 同步敏感配置到 K8s Secret，K8s Secret 仅作为运行时挂载载体；etcd 必须开启加密，Secret 不进入 Git、镜像、Helm values 明文和 CI 日志。非生产本地/测试环境可使用 `.env` 或本地 Secret 文件，但测试密钥也不得提交到 Git；如保留测试数据，应加密备份、限制文件权限并定期轮换。

```yaml
# === K8s Secret (由 External Secrets/Vault 同步，禁止手工提交明文) ===
apiVersion: v1
kind: Secret
metadata:
  name: hermes-secrets
type: Opaque
stringData:
  DB_PASSWORD: "xxx"
  REDIS_PASSWORD: "xxx"
  LLM_API_KEY: "sk-xxx"
  EMBEDDING_API_KEY: "sk-xxx"
  JWT_SECRET: "xxx"                    # >= 32 字符
  ENCRYPTION_KEY: "xxx"                # AES-256-GCM, 32字节 Base64
  MINIO_ROOT_PASSWORD: "xxx"
  RABBITMQ_PASSWORD: "xxx"
  LANGFUSE_SECRET_KEY: "xxx"

---
# === K8s ConfigMap (非敏感配置) ===
apiVersion: v1
kind: ConfigMap
metadata:
  name: hermes-config
data:
  MODEL_GATEWAY_URL: "http://model-gateway-svc:8080"
  MODEL_ROUTING_POLICY: "risk-control-prod-v1"
  MODEL_PROVIDER_REGISTRY: "deepseek,qwen,private-endpoint"
  EMBEDDING_API_BASE: "https://api.lingyaai.cn/v1"
  EMBEDDING_MODEL: "text-embedding-3-large"
  DB_HOST_WRITE: "pgbouncer-svc"
  DB_HOST_READ: "pgbouncer-read-svc"
  DB_PORT: "5432"
  REDIS_CLUSTER_NODES: "redis-0:6379,...,redis-5:6379"
  SEARCH_HOSTS: "search-svc:9200"
  VECTOR_STORE_URI: "milvus-svc:19530"
  RABBITMQ_HOST: "rabbitmq-svc"
  JAEGER_HOST: "jaeger-collector-svc"
  HOT_DATA_RETENTION_DAYS: "90"
  WARM_DATA_RETENTION_DAYS: "730"
  COLD_DATA_RETENTION_YEARS: "10"
  MAX_UPLOAD_SIZE_MB: "500"
  RATE_LIMIT_GLOBAL_RPS: "1000"
  RATE_LIMIT_USER_RPM: "100"
```

**身份与密钥治理基线**：

| 项 | 生产 K8s | 非生产 Docker Compose 本地/测试 |
|----|-------------|-------------------|
| 用户身份 | 企业 SSO/OIDC/AD 接入；关键操作（导出、机密下载、密钥轮换、管理员变更）启用 MFA | 可使用本地账号/JWT；管理员账号强密码；禁止使用生产账号密码 |
| 服务身份 | K8s ServiceAccount 最小权限；mTLS 或受控 NetworkPolicy 限制服务间访问 | Docker 网络隔离；管理端口仅本机/堡垒机访问 |
| 密钥存储 | Vault/External Secrets + K8s Secret（etcd 加密） | `.env`/本地 Secret 文件，权限 `600`，加密备份 |
| 密钥轮换 | DB/API Key 90 天；JWT 30 天；AES 180 天；TLS 证书自动续期 | 测试密钥至少每 180 天轮换；人员变更或泄露怀疑时立即轮换 |
| 应急账号 | break-glass 账号离线封存，启用审计和临时授权流程 | 本地管理员账号仅用于测试，禁止复用生产应急账号 |

### 6.8 CI/CD 流水线设计

```
┌────────────────────────────────────────────────────────────────────┐
│                        GitLab CI/CD 流水线                          │
│                                                                    │
│  ┌─────────┐    ┌─────────┐    ┌──────────┐    ┌──────────────┐   │
│  │ Stage 1 │───→│ Stage 2 │───→│ Stage 3  │───→│ Stage 4      │   │
│  │ Lint    │    │ Test    │    │ Build    │    │ Deploy       │   │
│  │         │    │         │    │          │    │              │   │
│  │ Ruff    │    │ pytest  │    │ Docker   │    │ K8s Rollout  │   │
│  │ MyPy    │    │ 单元测试 │    │ Build    │    │ 金丝雀→10%   │   │
│  │ ESLint  │    │ 集成测试 │    │ Harbor   │    │ →50%→100%   │   │
│  │ < 2min  │    │ < 5min  │    │ Push     │    │ 自动回滚     │   │
│  └─────────┘    └─────────┘    │ < 5min   │    │ < 15min      │   │
│                                 └──────────┘    └──────────────┘   │
│                                                                    │
│  ┌───────────────── 环境策略 ─────────────────────────────────┐    │
│  │  feature/* → Dev 环境 (K8s Namespace: hermes-dev)          │    │
│  │  develop   → Staging 环境 (hermes-staging, 生产镜像)       │    │
│  │  main      → 手动审批 → Production 金丝雀 → 全量          │    │
│  │  hotfix/*  → 快速通道 (跳过部分非关键测试)                   │    │
│  └────────────────────────────────────────────────────────────┘    │
│                                                                    │
│  ┌───────────────── 质量门禁 ──────────────────────────────────┐   │
│  │  ✓ 单元测试通过率 100% (新增代码覆盖率 ≥ 80%)               │   │
│  │  ✓ 集成测试通过率 100%                                      │   │
│  │  ✓ Lint 零错误 (Ruff/MyPy/ESLint)                          │   │
│  │  ✓ 安全扫描无高危漏洞 (Trivy 镜像扫描 + Bandit SAST)       │   │
│  │  ✓ SBOM 生成 + 镜像签名 + Secret scanning + IaC 扫描        │   │
│  │  ✓ 数据库迁移无冲突 (alembic check)                        │   │
│  │  ✓ 构建产物: Docker Image(不可变 digest) + Helm Chart       │   │
│  └────────────────────────────────────────────────────────────┘    │
│                                                                    │
│  ┌───────────────── 回滚策略 ──────────────────────────────────┐   │
│  │  金丝雀期间 (10% 流量): HPA 指标恶化 (P95 > 1s) → 自动回滚 │   │
│  │  全量部署后: 监控 30 分钟，错误率 > 5% → 自动回滚           │   │
│  │  手动回滚: kubectl rollout undo；DB 迁移按前滚/回滚脚本处理 │   │
│  │  回滚时间目标: < 5 分钟                                     │   │
│  └────────────────────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────────────────┘
```

**供应链安全门禁**：

| 门禁 | 要求 |
|------|------|
| SBOM | Build 阶段生成镜像和前后端依赖 SBOM，随发布制品归档 |
| 镜像签名 | Harbor 中生产镜像必须使用不可变 digest，并通过签名验证后才允许部署 |
| Secret scanning | MR 与 CI 均扫描密钥泄露；命中后阻断流水线并轮换已泄露密钥 |
| IaC 扫描 | Helm/K8s YAML 检查 privileged、hostPath、root 用户、无 requests/limits 等高危配置 |
| Admission Policy | 生产 Namespace 使用 OPA Gatekeeper/Kyverno 拒绝未签名镜像和越权 Pod |
| DB 回滚边界 | 数据库变更不依赖自动恢复快照作为常规回滚；高风险迁移必须提供前滚修复脚本、只读验证和人工审批 |

---

## 七、模块间交互设计

### 7.1 数据流全景（消息驱动架构）

```
                        ┌───────────────────┐
                        │    2. 风险监控      │
                        │   (主动异常扫描)    │
                        │   ┌─────────────┐  │
                        │   │ 7x24 执行    │  │
                        │   │ 异常数据生成  │  │
                        │   └──┬───┬───┬──┘  │
                        └──┬───┼───┼───┼──┬──┘
                           │   │   │   │  │
             ┌─────────────┘   │   │   │  └─────────────┐
             ↓ 异常推送→立案    │   │   │ 异常推送→行为核查 ↓
      ┌──────────────┐         │   │   │         ┌──────────────┐
      │ 1. 廉洁监察   │         │   │   │         │ 7. 行为风险   │
      │  ┌─────────┐ │         │   │   │         │ ┌──────────┐ │
      │  │6阶段工作流│ │         │   │   │         │ │行为分析  │ │
      │  │A2A→龟宝  │ │         │   │   │         │ │管理报告  │ │
      │  └────┬────┘ │         │   │   │         │ └────┬─────┘ │
      └───────┼──────┘         │   │   │         └──┬───┼───────┘
              │                │   │   │            │   │
              │    ┌───────────┘   │   └─────┐      │   │
              │    │               │         │      │   │ 预警→问题清单
              │    ↓ 异常推送→预警   ↓ 异常推送→核查  │   ↓
              │  ┌──────────┐ ┌──────────┐         │   │
              │  │6.商业秘密 │ │3.内控评价 │         │   │
              │  │ ┌──────┐ │ │ ┌──────┐ │         │   │
              │  │ │定密预审│ │ │ │13步骤│ │         │   │
              │  │ │定密评审│ │ │ │制度库 │ │         │   │
              │  │ │管理报告│ │ │ │19循环│ │         │   │
              │  │ └──┬───┘ │ │ └──┬───┘ │         │   │
              │  │    │     │ │    │     │         │   │
              │  └────┼─────┘ └────┼─────┘         │   │
              │       │ 制度库 ←───┘               │   │
              │       └──────────┐调用制度库比对     │   │
              │                  │                 │   │
              ↓    ↓   问题清单汇入（统一整改入口，消息队列驱动） ↓   ↓
      ┌─────────────────────────────────────────────────────┐
      │                    8. 持续改善                        │
      │  ┌─────────────────────────────────────────────┐   │
      │  │ 统一整改承接 → AI初审 → 任务下发 → 跟踪       │   │
      │  │ → 二次复核 → 闭环归档 → 统计分析             │   │
      │  └─────────────────────────────────────────────┘   │
      └─────────────────────────────────────────────────────┘
                 ↑                    ↑
        ┌────────┴────────┐  ┌───────┴────────┐
        │   4. 专项审计    │  │   5. 离任审计    │
        │  ┌────────────┐ │  │  ┌────────────┐ │
        │  │5阶段工作流  │ │  │  │6阶段工作流  │ │
        │  │方案→检查→报告│ │  │  │个人+业务问题│ │
        │  └────────────┘ │  │  └────────────┘ │
        └─────────────────┘  └─────────────────┘
                             (接收行为风险预警结果)
```

### 7.2 模块间数据流详表

| 上游模块 | 下游模块 | 数据类型 | 交互频率 | 传输方式 | 数据量级 |
|----------|----------|----------|----------|----------|----------|
| **风险监控 → 廉洁监察** | 异常推送（舞弊风险事件） | 案件线索 | 准实时（事件触发） | RabbitMQ 消息 + DB写入 | 少量(KB) |
| **风险监控 → 内控评价** | 合规异常推送 | 异常明细 | 准实时 | RabbitMQ 消息 | 少量(KB) |
| **风险监控 → 商业秘密** | 泄密风险预警 | 预警事件 | 准实时 | RabbitMQ 消息 | 少量(KB) |
| **风险监控 → 行为风险** | 异常推送（行为异常事件） | 行为异常数据 | 准实时 | RabbitMQ 消息 | 少量(KB) |
| **行为风险 → 离任审计** | 行为风险预警结果 | 预警数据 | 审计执行前 | API 查询 | 少量(KB) |
| **行为风险 → 持续改善** | 问题清单（异常行为需整改项） | 结构化问题条目 | 报告出具时 | DB写入 + 消息通知 | 少量(KB) |
| **廉洁监察 → 持续改善** | 问题清单（处罚、整改建议） | 结构化问题条目 | 阶段完成时 | RabbitMQ 消息 + DB写入 | 少量(KB) |
| **内控评价 → 持续改善** | 缺陷清单（设计缺陷+执行缺陷） | 结构化缺陷条目 | 报告出具时 | DB写入 + 消息通知 | 少量(KB) |
| **内控评价 → 商业秘密** | 制度库（共享依赖） | 制度文件 | 按需查询 | API查询 + 消息队列批量同步 | 大量(MB-GB) |
| **专项审计 → 持续改善** | 审计问题清单 | 结构化问题条目 | 报告出具时 | DB写入 + 消息通知 | 少量(KB) |
| **离任审计 → 持续改善** | 问题清单（个人+业务） | 结构化问题条目 | 报告出具时 | DB写入 + 消息通知 | 少量(KB) |
| **离任审计 ← 行为风险** | 行为风险预警结果 | 预警数据 | 审计执行前 | API 查询 | 少量(KB) |
| **全模块 ↔ 多模态处理层** | 文件上传 → 处理结果 | 音频/图像/视频/文档 | 文件上传时异步触发 | MinIO 事件 → RabbitMQ → Worker → Search Adapter + Milvus | 大量(MB-GB/文件) |

> **传输方式升级说明**：模块间数据交互从"直接 API 调用 + 数据库写入"升级为"**RabbitMQ 消息驱动 + 数据库最终一致性**"。大文件传输（如制度库同步）通过 MinIO 预签名 URL 或消息队列元数据交换，避免大载荷阻塞消息总线。

### 7.3 A2A 智能体通信

#### 7.3.1 通信架构

```
┌───────────────────────────────────────────────────────────────────┐
│                       A2A Bus (基于 RabbitMQ 的消息总线)              │
│                                                                    │
│  ┌───────────────────┐          ┌─────────────────────────────┐   │
│  │   Hermes (赫尔墨斯) │          │   外部智能体                    │   │
│  │                    │          │                              │   │
│  │  ┌───────────────┐ │  A2A     │  ┌─────────┐  ┌─────────┐  │   │
│  │  │ 廉洁监察模块    │─┼────────→│  │  龟宝    │  │  西塞罗  │  │   │
│  │  │ (HR案件转交)   │ │ Protocol│  │ (HR处罚) │  │ (法务)   │  │   │
│  │  └───────────────┘ │←────────┼──│          │  │         │  │   │
│  │                    │          │  └─────────┘  └─────────┘  │   │
│  │  ┌───────────────┐ │          │                              │   │
│  │  │ 廉洁监察模块    │─┼────────→│  ┌─────────┐               │   │
│  │  │ (供应商扣款)    │ │          │  │  波特    │               │   │
│  │  └───────────────┘ │          │  │ (财务)   │               │   │
│  └───────────────────┘          │  └─────────┘               │   │
│                                  └─────────────────────────────┘   │
│                                                                    │
│  交互模式:                                                          │
│  ├── 请求-回调: Hermes 发送任务 → 外部Agent异步处理 → 回调 Webhook  │
│  ├── 查询-响应: Hermes 主动查询外部Agent状态                       │
│  └── 消息保证:                                                     │
│      ├── quorum queues + Publisher Confirm + 消费 ACK              │
│      ├── 重试 3 次 → DLQ → replay queue / 人工介入                  │
│      ├── Outbox/Inbox 持久化事件状态，保证业务写入与消息发布一致       │
│      └── 幂等消费: idempotency_key 去重 (Redis TTL + PG Inbox)      │
└───────────────────────────────────────────────────────────────────┘
```

#### 7.3.2 A2A 任务消息协议（统一格式）

```json
{
  "schema_version": "a2a.task.v1",
  "event_id": "uuid",
  "task_id": "uuid",
  "correlation_id": "trace/task correlation id",
  "idempotency_key": "target_agent:command:case_ref:payload_hash",
  "source_agent": "hermes",
  "target_agent": "guibao | cicero | porter",
  "command": "操作指令",
  "case_ref": "案件编号",
  "priority": "normal | high | critical",
  "payload_schema": "uri-or-name",
  "payload": { /* 业务载荷 */ },
  "callback_url": "https://hermes/api/v1/webhooks/a2a/{agent}",
  "callback_queue": "hermes.a2a.callback.{agent}",
  "created_at": "ISO8601",
  "expires_at": "ISO8601 (TTL 24h)",
  "retry_count": 0,
  "signature": "HMAC-SHA256 over canonical body"
}
```

**消息契约与重放规则**：

| 项 | 规则 |
|----|------|
| 版本 | 所有 A2A 和内部事件必须带 `schema_version`；同一主版本内只允许向后兼容新增字段 |
| 幂等 | 生产同时使用 Redis TTL 去重和 PostgreSQL Inbox 落库；Redis 失效不影响长期去重审计 |
| Outbox | 业务事务内写入 Outbox，后台发布到 RabbitMQ；发布成功后标记 `published_at` |
| DLQ | poison message 进入 DLQ 后保留 30 天；P1 需在 4 小时内确认处理策略 |
| Replay | 重放必须指定 `event_id` 范围、操作者和原因；重放动作写入 audit_log |
| Webhook | 外部回调必须校验 HMAC 签名、时间戳窗口和来源 IP 白名单 |

#### 7.3.3 A2A 交互矩阵

| Hermes 模块 | 目标 Agent | 方向 | 命令 | 触发条件 | 优先级 |
|-------------|-----------|------|------|----------|--------|
| 廉洁监察 | **龟宝** | 推→ | `initiate_penalty_tracking` | 员工处罚需要落实跟踪 | normal |
| 廉洁监察 | **龟宝** | 推→ | `transfer_hr_case` | HR 相关案件转交 | high |
| 廉洁监察 | **龟宝** | ←收 | `penalty_update` | 龟宝回传员工扣款/处罚结果 | normal |
| 廉洁监察 | **西塞罗** | 推→ | `push_civil_case` | 民事纠纷案件推送法务 | high |
| 廉洁监察 | **西塞罗** | 推→ | `submit_agreement_review` | 赔偿协议推送法务审核 | normal |
| 廉洁监察 | **西塞罗** | ←收 | `review_result` | 法务审核结果回传 | normal |
| 廉洁监察 | **波特** | 推→ | `initiate_supplier_deduction` | 供应商扣款任务推送 | normal |
| 廉洁监察 | **波特** | ←收 | `deduction_status` | 波特回传扣款进度 | normal |
| 内控评价 | **西塞罗** | 查→ | `query_position_responsibility` | 访谈人员匹配时查询岗位职责 | normal |

### 7.4 外部系统数据流

```
┌──────────────────────────────────────────────────────────────────┐
│                        外部系统数据流                              │
│                                                                   │
│  ┌─────────────┐ 字段映射同步 (消息队列)  ┌──────────────────────┐ │
│  │  风控系统     │←──────────────────────→│   Hermes                │ │
│  │  ├── 案件创建  │   WebSocket 状态推送    │   ├── 案件信息同步      │ │
│  │  ├── 状态同步  │                        │   ├── 初判结果回写      │ │
│  │  ├── 按钮联动  │                        │   ├── 处置结果回写      │ │
│  │  └── 附件上传  │                        │   └── 字段自动填充      │ │
│  └─────────────┘                          └──────────────────────┘ │
│                                                                   │
│  ┌─────────────┐  审批推送/回调 (消息队列) ┌──────────────────────┐ │
│  │  OA 系统     │←──────────────────────→│   Hermes                │ │
│  │  (BPM流程)   │                        │   ├── 处罚公告审批推送  │ │
│  │              │                        │   └── 审批状态回调      │ │
│  └─────────────┘                          └──────────────────────┘ │
│                                                                   │
│  ┌─────────────┐ 黑名单同步 (消息队列)     ┌──────────────────────┐ │
│  │  MDM 系统    │←───────────────────────│   Hermes                │ │
│  │  (主数据管理) │                        │   ├── 供应商黑名单导入  │ │
│  │              │                        │   └── 组织架构查询      │ │
│  └─────────────┘                          └──────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

### 7.5 调用链路（以廉洁监察完整流为例，含多模态处理）

```
用户操作 (风控系统按钮)
    │
    ▼
API 层: POST /api/v1/cases (创建案件, 生成 task_id) → 返回 { task_id }
    │
    ├── 附件上传 (并行): POST /api/v1/media/upload
    │   ├── MinIO 直接上传 (预签名 URL, 支持断点续传, 最大 500MB)
    │   ├── MinIO Bucket 事件 → RabbitMQ (@audio/@image/@video/@doc_queue)
    │   ├── 多模态 Worker 异步处理 → 提取文本 → Search Adapter + Milvus 索引
    │   └── 处理完成 → WebSocket 推送 "{file_id} 处理完成"
    │
    ▼
API 层: POST /api/v1/cases/{id}/workflow/start → 返回 { task_id: "wf-xxx" }
    │
    ▼
Service 层: WorkflowService.start_workflow() 仅创建 workflow 命令与业务事实
    │
    ▼
Workflow Runtime: 读取命令 → graph.invoke/resume(state, config={"configurable": {"thread_id": ...}})
    │
    ├── [intake 节点]
    │   ├── KB 检索 (Search Adapter + Milvus + metadata filter + rerank) → 组织架构、制度法规
    │   ├── Search Adapter 全文检索: 历史相似案件
    │   ├── LLM 推理 (Model Gateway, llm_queue 优先级队列) → 结构化初判 + 分流建议
    │   ├── State 写入 → PostgreSQL durable checkpointer + Redis 热缓存
    │   └── interrupt_before → 挂起等待人工守门 → WebSocket 推送
    │
    ├── [用户守门]
    │   ├── API: POST /api/v1/cases/{id}/approval/intake
    │   ├── 守门结果写入 audit_log (不可篡改) + ES 审计索引
    │   └── Workflow Runtime resume：将人工修改后的内容作为下游节点输入
    │
    ├── [条件路由: route_after_intake]
    │   ├── 不处理 → close_case_not_handled → 风控同步 → END
    │   ├── 转交HR → transfer_to_hr → A2A→龟宝 (a2a_queue) → END
    │   └── 继续调查 → investigation 节点 →
    │
    ├── [investigation → analysis → disposition]
    │   ├── 每阶段: KB检索 → LLM推理 → 结构化 stage_output → interrupt → 守门 → resume
    │   ├── investigation: 调查方案生成任务 (→ report_queue, Celery异步；完成事件恢复 workflow)
    │   ├── analysis: 多维数据分析 + 报告撰写
    │   │   ├── Tool: SQL数据分析 (sync_queue, Celery异步)
    │   │   ├── Tool: 语音转文字查询 (多模态结果, ES+PG)
    │   │   ├── Tool: 全文检索证据 (Elasticsearch/OpenSearch)
    │   │   ├── Tool: 图像/证据相似检索 (Milvus 向量检索)
    │   │   └── Tool: 报告生成 (report_queue, MinIO存储, CDN分发)
    │   └── disposition: 追责分流
    │       └── 条件路由: 不追责 → END | 追责 → enforcement
    │
    ├── [enforcement]
    │   ├── A2A→龟宝: 员工处罚 (a2a_queue)
    │   ├── A2A→西塞罗: 协议审核 (a2a_queue)
    │   ├── A2A→波特: 供应商扣款 (a2a_queue)
    │   ├── MDM Adapter: 黑名单同步 (sync_queue)
    │   ├── OA Adapter: 处罚公告审批 (sync_queue)
    │   └── interrupt → 守门 → resume
    │
    └── [post_report]
        ├── 报案协助 → 资料提取指引
        ├── 触发数据生命周期检查 (案件关闭 → 热→温迁移倒计时)
        └── END (闭环)
```

---

## 八、非功能性需求设计

### 8.1 安全性设计（等保二级 + 10K 规模增强）

| 安全域 | 需求项 | 实现方式 |
|--------|--------|----------|
| **身份鉴别** | 用户身份唯一标识与验证 | P1 接入企业 SSO/OIDC/AD；JWT 令牌机制，Access Token 8h/Refresh Token 7天；失败 5 次锁定 30 分钟；关键操作启用 MFA；Token 黑名单 (Redis Cluster 全局同步) |
| **访问控制** | 基于角色的细粒度权限 | RBAC 三级角色（集团/科沃斯/添可）；生产默认启用 `client` 行级过滤 + PG RLS；大文档场景支持字段级权限 |
| **安全审计** | 全量操作可追溯 | 所有操作记录 `audit_log` 表（操作人/IP/时间/内容/结果）；日志 append-only，不可删除；ES 索引保留 1 年可搜索，归档保留 10 年 |
| **通信保密性** | 传输层加密 | HTTPS (TLS 1.2+)；内网服务间 mTLS；API 限流 (全局 1000/s + 用户 100/min) |
| **数据保密性** | 敏感字段加密存储 | AES-256-GCM 列级加密（姓名/电话/邮箱）；P1 密钥由 Vault/External Secrets 注入；MinIO SSE-S3 文件加密 |
| **数据分级保护** | 10TB 数据分类管控 | 密级标记：公开/内部/秘密/机密四级；敏感文件访问独立审计；机密级下载需风控负责人审批 |
| **AI 安全** | 防止 LLM 越权和幻觉 | Prompt 注入防护、RAG 租户过滤、Tool 权限矩阵、法规/证据引用强制校验；高风险输出进入人工复核 |
| **软件容错** | 关键节点失败处理 | StageNode 最大重试 3 次 → human_intervention → 告警+人工接管；RabbitMQ 自动重试 + DLX 死信队列 |
| **资源控制** | 防止资源滥用 | 混合限流（全局 1000/s + 用户 100/min + LLM 令牌桶 50/s）；文件上传 500MB；PgBouncer pool_size=500 |
| **数据备份** | 数据可恢复 | PG 每日全量 + WAL 连续归档 (MinIO + NAS 双副本)；Redis AOF+RDB；MinIO EC 12+4 冗余 |
| **剩余信息保护** | 登出时清除会话 | JWT 黑名单 (Redis Cluster 全局同步)；敏感字段不输出到日志 |

### 8.2 可扩展性设计

| 维度 | 扩展策略 | 目标状态 |
|------|----------|----------|
| **API 服务** | 无状态设计，K8s HPA 自动扩缩 | 3-20 实例，Nginx least_conn 负载均衡 |
| **Worker 服务** | 按任务类型分 9 个独立池，各池独立 HPA | audio/img/video/doc/report/sync/a2a/llm/kb 独立扩缩 |
| **数据库** | 读写分离 + 分区表 + 连接池 | Primary + 2 Read Replicas + PgBouncer (pool_size=500) |
| **数据库分区** | 按年+客户分区 | cases(RANGE年), audit_logs(RANGE月), evidences(HASH case_id) |
| **Redis** | 集群模式 | 3 主 3 从 Cluster，自动故障转移 + 分片 |
| **MinIO** | 多节点集群 | 4 节点 EC 12+4，支持横向添加节点 |
| **Elasticsearch/OpenSearch** | 分布式全文检索集群 | 3+ 节点，分片和副本按索引容量规划，通过 Search Adapter 访问 |
| **Milvus** | 分布式向量检索集群 | 知识库、证据、历史案例向量召回，集合按模块和租户隔离 |
| **RabbitMQ** | quorum queues 集群 | 3 节点，消息持久化，DLX 死信，支持 replay queue |
| **模块扩展** | 新模块复用基础设施 | 8 模块基于共享底座，独立 K8s namespace |
| **LLM 切换** | LLMAdapter 抽象层 + 优先级队列 | 新增 LLM 仅需 Adapter；按优先级调度 |
| **A2A Agent** | A2A Bus + Adapter 模式 | 新增 Agent 仅需 Adapter + routing key |

### 8.3 性能设计

| 场景 | 性能指标 | 实现策略 |
|------|----------|----------|
| **API 响应时间** | P50 < 200ms, P95 < 500ms, P99 < 1s | 异步 FastAPI + asyncpg + Redis Cluster 热点缓存 (5min TTL) |
| **多模态任务提交** | < 100ms (返回 task_id) | 文件上传 → MinIO 事件 → RabbitMQ → 异步 Worker；WebSocket 推送进度 |
| **音频处理 (1h)** | < 5min | Whisper large-v3 + V100 GPU；分片并行解码 |
| **图像单页 OCR** | < 2s | PaddleOCR GPU + 预热模型 |
| **100 页 PDF (扫描件)** | < 3min | PaddleOCR 批量并行 (4页/批)；电子原生 < 2min |
| **10 分钟视频关键帧** | < 30s | FFmpeg 场景变化检测 (1fps) |
| **LLM 单次调用** | 3-15s | llm_queue 优先级队列；超时 30s → 重试 → 降级备用 LLM |
| **知识库检索 (混合)** | P95 < 200ms | Elasticsearch/OpenSearch 全文召回 + Milvus 向量召回 + metadata filter + rerank；权重可配 |
| **并发** | 10K 注册/授权用户，峰值 500 并发 | PgBouncer pool_size=500；API HPA 3-20；LLM 令牌桶 50/s |
| **大文件上传** | 最大 500MB (分片) | MinIO 预签名 URL；分片上传 (5MB/chunk) + 断点续传 |
| **大文件下载** | 支持 > 100MB 流式 | HTTP Range；视频 HLS 自适应码率；PDF 分页流式 |
| **文档预览缩略图** | 生成 < 2s/页, CDN < 50ms | 文档解析时预生成缩略图 → MinIO → Nginx CDN (24h TTL) |

### 8.4 可用性设计

**全局可用性目标**：99.9%（内部系统，年停机 ≤ 8.76 小时）

| 场景 | 策略 | RTO | 降级行为 |
|------|------|-----|----------|
| **LLM 不可用** | Model Gateway 自动熔断主 provider 并切换备用 provider；全部不可用 → human_intervention，任务保留队列 | 0s | 降级：案件进入人工接管模式 |
| **DB 主库不可用** | Patroni 自动故障转移；Replica 提升为 Primary | < 30s | 降级：写操作暂停(队列缓冲) |
| **DB 读副本不可用** | 读请求路由到 Primary 或其他 Replica | 0s | 降级：性能下降 (Primary 承载读写) |
| **Redis 部分不可用** | Cluster 自动故障转移 | < 30s | 降级：缓存、会话和限流能力短暂受影响；workflow 以 PostgreSQL durable checkpoint 恢复 |
| **ES 不可用** | 全文搜索降级到 PostgreSQL FTS | 0s | 降级：搜索变慢 (P95 可能 > 1s) |
| **RabbitMQ 不可用** | quorum queue leader 自动转移；KEDA 暂停扩容并保留任务提交降级开关 | < 10s | 降级：新任务无法提交，已持久化任务继续恢复消费 |
| **MinIO 部分节点** | EC 12+4 容忍 4 节点故障 | 0s | 降级：读取可用 (EC 恢复) |
| **外部系统不可用** | Adapter 熔断器 (5 失败 → OPEN 60s → HALF_OPEN)；> 5min → P2 告警 | 0s | 降级：同步操作延后 |
| **多模态管道不可用** | 文件正常上传，处理任务保留队列 | 0s | 降级：文件已上传待处理 |
| **工作流中断** | PostgreSQL durable checkpointer 持久化；thread_id 精确恢复；Redis 仅作热缓存 | < 10s | 无降级 |
| **节点故障** | K8s liveness probe 自动重启不健康 Pod | < 30s | Pod 迁移到健康节点 |

### 8.5 可观测性设计

```
┌─────────────────────────────────────────────────────────────────────┐
│                OpenTelemetry 统一采集 + 可观测性四支柱                   │
│                                                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────┐ │
│  │  Logs (日志)  │  │Metrics (指标)│  │Traces (追踪) │  │Business  │ │
│  │  structlog    │  │  Prometheus  │  │   Jaeger     │  │  Metrics │ │
│  │              │  │              │  │              │  │  Grafana │ │
│  │  结构化JSON   │  │  API QPS/P95 │  │  跨服务调用链 │  │  Dashboard│ │
│  │  traceId 关联 │  │  DB 连接池   │  │  瓶颈分析    │  │          │ │
│  │  敏感字段脱敏 │  │  Redis 命中率 │  │  服务依赖图  │  │  用户活跃 │ │
│  │  不可删除    │  │  RabbitMQ 深度│  │  采样策略    │  │  案件处理 │ │
│  │  保留 >= 1年  │  │  Celery 统计  │  │  (正常10%)   │  │  量/趋势  │ │
│  │  (ES 索引)   │  │  K8s Pod 资源 │  │  (错误100%)  │  │  存储使用 │ │
│  └──────────────┘  │  GPU 利用率   │  └──────────────┘  │  趋势      │ │
│                     │  ES 查询延迟   │                    │  处理吞吐 │ │
│                     │  速率限制触发  │                    └──────────┘ │
│                     └──────────────┘                                  │
│                                                                      │
│  ┌───────────────────── 告警规则 ─────────────────────────────────┐  │
│  │  P0 紧急: API P95 > 2s / PG Primary DOWN                      │  │
│  │  P1 严重: LLM 10次失败 / API 5xx > 5% / RabbitMQ磁盘 > 85%     │  │
│  │  P2 警告: KB检索空 > 10% / 外部系统不可用 / ES延迟 > 500ms    │  │
│  │  P3 提醒: 案件停滞 > 24h / 队列深度 > 1000 / 冷存储 < 10%      │  │
│  └────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

### 8.6 业务连续性设计

| 场景 | RPO | RTO | 实现方式 |
|------|-----|-----|----------|
| **数据库主库故障** | < 5min | < 15min | Patroni 自动故障转移 + PgBouncer 切换 |
| **数据库集群全故障** | < 1min (WAL最后归档) | < 2h | 从 MinIO + NAS 备份 pgBackRest 完整恢复 |
| **Redis 全集群故障** | < 1min (AOF最后同步) | < 15min | AOF+RDB 持久化；Cluster 自动重建 |
| **MinIO 集群全故障** | < 1h (最后备份) | < 1h | EC 冗余 + NAS 定期备份 (rsync) |
| **ES 全集群故障** | < 1h | < 1h | 快照备份到 MinIO (每日) → 快照恢复 |
| **RabbitMQ 集群故障** | 0 (已确认持久化消息) | < 15min | quorum queues + 磁盘持久化 + DLQ/replay queue |
| **应用 (API/Worker)** | 0 (无状态) | < 1min | K8s 多副本 + 健康检查 + 自动重启 + HPA |
| **完整站点灾难** | < 1h | < 4h | 异地备份 (MinIO + NAS 跨机房)；季度灾备演练 |

**灾备演练要求**：每季度一次；范围含 DB 恢复 + Redis 恢复 + ES 索引重建 (必做) + 完整站点切换 (年度选做)；每次输出 RPO/RTO 实际值 + 改进计划。

### 8.7 多模态处理性能设计

| 处理类型 | 输入 | 处理时间目标 | 实现方式 | Worker 配置 |
|----------|------|-------------|----------|------------|
| **音频转文字** | 1h 录音 (16kHz单声道) | < 5min | Whisper large-v3 + V100, 分片并行(30s/segment) | celery-audio, GPU, 1-3 pods |
| **音频转文字** | 10min 语音备忘录 | < 1min | faster-whisper CTranslate2 | celery-audio |
| **说话人分离** | 1h 多人访谈 | < 2min | pyannote-audio + GPU | celery-audio (与ASR串联) |
| **单页 OCR** | A4 扫描件 (300dpi) | < 2s | PaddleOCR GPU, 模型预热 | celery-image, GPU, 1-3 pods |
| **100 页 PDF (扫描)** | 合同扫描件 | < 3min | 分页并行 (4页/批, 25批) | celery-image |
| **100 页 PDF (电子)** | Word导出PDF | < 2min | unstructured.io 直接解析 | celery-doc, CPU, 2-5 pods |
| **图像分类** | 单张证据图片 | < 1s | CLIP/ViT 零样本分类 | celery-image |
| **视频关键帧** | 10min 监控 (1080p) | < 30s | FFmpeg 场景检测 (1fps) | celery-video, GPU, 1-2 pods |
| **视频完整分析** | 10min 访谈视频 | < 3min | 关键帧+OCR+音频转录 (并行) | celery-video |
| **视频代理生成** | 10min → 480p | < 2min | FFmpeg NVENC 硬件编码 + HLS切片 | celery-video |
| **文档解析** | 50页 Word报告 | < 30s | unstructured.io + python-docx | celery-doc |
| **文档分块+索引** | 100页 PDF | < 1min | Chunking + Embedding + Search Adapter + Milvus | celery-kb, CPU, 1-3 pods |

**处理优先级**：证据材料（活跃案件相关）> 批量归档处理 > 历史数据回溯处理。优先队列实现于 RabbitMQ 消息优先级。

### 8.8 数据生命周期管理

| 阶段 | 触发条件 | 迁移动作 | 存储介质 | 访问 SLA |
|------|----------|----------|----------|----------|
| **热层 (Hot)** | 案件创建至关闭后 90 天 | — | PostgreSQL 业务事实库 + Search/Milvus 检索域 + MinIO 对象热访问 | P95 < 50ms (索引) / < 200ms (复杂) |
| **热→温** | 案件关闭后 90 天 | 案件完整数据打包 → MinIO；PG 中保留元数据指针和摘要；原始大文件从 PG 删除 | MinIO 对象存储 | P95 < 200ms (元数据) / < 1s (文件下载) |
| **温→冷** | 案件关闭后 2 年 | 案件 + 证据 + 报告完整打包 → NAS/磁带；MinIO 中删除；ES 索引保留可搜索元数据；PG 仅保留案件号、归档路径、关键词 | NAS / 磁带 | 按需恢复：< 24h |
| **冷保留** | 冷层已存储 | 保留 10 年（法规要求） | NAS / 磁带 | 按需恢复 |
| **到期清理** | 超过 10 年 | 审批后物理销毁 | — | — |

**自动化机制**：
- 每日定时任务扫描案件状态：`status=closed AND closed_at < now() - 90days` → 热→温迁移
- 每周定时任务：`closed_at < now() - 2years` → 温→冷迁移
- 冷恢复流程：用户 API 请求 → 后台创建恢复任务 → 机器人从磁带提取 → 暂存 MinIO → 通知用户下载 (7 天有效期)
- 迁移日志完整记录到 audit_log

### 8.9 测试策略

**测试金字塔**：

```
                  ┌──────────────┐
                  │  E2E / 混沌   │  10 场景 (Playwright + Chaos Mesh)
                  │  < 30min     │  金丝雀部署后自动执行
                 ┌┴──────────────┴┐
                 │   集成测试      │  200+ 场景 (pytest-asyncio + TestClient)
                 │   < 10min     │  API 全端点覆盖 + 工作流端到端
                ┌┴───────────────┴┐
                │    单元测试      │  1000+ 用例 (pytest)
                │    < 2min      │  覆盖率 ≥ 80% (核心模块 ≥ 90%)
               ┌┴────────────────┴┐
               │   静态分析        │  Ruff + MyPy(strict) + ESLint
               │   < 1min        │  CI 门禁 (零容忍)
               └─────────────────┘
```

**测试类型覆盖**：

| 测试类型 | 工具 | 范围 | CI 阶段 | 频率 |
|----------|------|------|---------|------|
| **静态分析** | Ruff, MyPy, ESLint | 全量代码 | Lint (Stage 1) | 每 commit |
| **单元测试** | pytest + pytest-asyncio | 所有纯函数/类方法 | Test (Stage 2) | 每 commit |
| **API 契约测试** | pytest + httpx TestClient | 所有 API 端点 | Test (Stage 2) | 每 commit |
| **集成测试** | pytest + Docker Compose | DB/Redis/MQ 交互 | Test (Stage 2) | 每 MR |
| **工作流测试** | pytest + Mock LLM | 8 个模块 LangGraph 流程 | Test (Stage 2) | 每 MR |
| **多模态处理测试** | pytest + 样本文件 (音频/图像/视频/文档) | 4 条处理管道 | Build (Stage 3) | 每 MR |
| **性能测试** | Locust (500 峰值并发 + 10K 用户数据模型) | API 吞吐/延迟 | 手动/发版前 | 每版本 |
| **安全扫描** | Trivy + Bandit + npm audit + Secret/IaC scanning | 镜像/依赖/代码/配置 | Build (Stage 3) | 每 MR |
| **AI 安全测试** | Prompt 注入红队 + 黄金评测集 | RAG 越权、工具越权、幻觉、引用准确性 | Deploy-Staging | 每版本 |
| **E2E 测试** | Playwright | 核心用户旅程 | Deploy-Staging | 部署后 |
| **混沌测试** | Chaos Mesh (K8s) | 故障注入 (Pod Kill/网络分区/磁盘故障) | 手动 | 每季度 |

**测试数据管理**：
- 单元/集成测试：使用 fixture 工厂 + SQLite 内存库 (fast)
- E2E 测试：使用脱敏后的生产数据子集 (anonymized DB dump)
- 多模态测试：预置标准样本库 (标准音频/扫描件/视频片段)
- 性能测试：数据生成器模拟 10TB 规模写入负载

**生产架构验收场景**：

| 场景 | 验收要求 |
|------|----------|
| HITL 长暂停恢复 | 任意 HITL 节点暂停 24 小时后，workflow 能从 durable checkpoint 精确恢复。 |
| 人工修改传递 | 人工修改阶段输出后，下游 Agent 使用修改后的内容，而不是原始 AI 输出。 |
| Worker 幂等 | Worker 崩溃、Pod 重启、RabbitMQ 重投递时，不产生重复报告、重复处罚、重复外部推送。 |
| 外部回调去重 | 外部系统重复回调时，Inbox 去重生效，业务状态只推进一次。 |
| 模型熔断 | LLM 主 provider 不可用时，Model Gateway 熔断并切换备用 provider；双 provider 不可用时进入人工接管。 |
| 检索降级 | Elasticsearch/OpenSearch 不可用时，案件主流程不丢状态；向量库不可用时，Agent 进入“知识不足/人工接管”而非编造结论。 |
| 审计追溯 | 任意案件状态变化都能追踪操作者、AI 输出、知识来源、人工意见、trace_id 和时间戳。 |
| 安全失败 | 租户越权查询、敏感字段明文泄漏、对象存储直链泄漏必须在安全测试中失败。 |

### 8.10 零停机数据库迁移策略

| 迁移类型 | 策略 | 示例 | 风险等级 |
|----------|------|------|----------|
| **新增表** | 直接执行，无影响 | `CREATE TABLE new_feature (...)` | 低 |
| **新增列 (可空)** | 直接执行；应用代码先兼容新列，再填充数据 | `ALTER TABLE ADD COLUMN x TEXT` | 低 |
| **新增列 (非空+默认值)** | 分三步：① 加可空列 + 默认值填充 ② 数据回填 ③ 改 NOT NULL | `ALTER TABLE ADD COLUMN status VARCHAR DEFAULT 'pending'` | 中 |
| **修改列类型** | 新增列 → 双写 → 回填 → 切换读取 → 删除旧列 | `VARCHAR(50)` → `VARCHAR(100)` | 高 |
| **删除列/表** | 先标记废弃 (应用代码不再读写) → 下一版本删除 | 软删除周期：至少 1 个版本 | 高 |
| **索引变更** | PostgreSQL `CONCURRENTLY` 模式 (不锁表) | `CREATE INDEX CONCURRENTLY idx_xxx ON t(c)` | 低 |
| **数据迁移** | Celery 异步任务分批处理 (1K/批, 间隔 100ms) | 历史数据重新分块 → Embedding | 中 |

**迁移执行流程**：
1. Alembic 生成迁移脚本 → Code Review (重点: 锁表风险、回滚方案)
2. Staging 环境验证 (使用生产数据量级模拟)
3. 生产维护窗口执行 (凌晨 2:00-4:00，或 CONCURRENTLY 随时执行)
4. 预检查: `SELECT pg_size_pretty(pg_database_size('hermes'))`, 磁盘剩余 > 30%
5. 迁移执行 + 监控 (长时间锁检测告警: > 10s)
6. 验证: 冒烟测试套件 (10 个核心 API)

**回滚边界**：生产数据库迁移不以"恢复上一版本 DB 快照"作为常规回滚手段。低/中风险迁移必须提供前滚修复脚本；高风险迁移必须提供双写/回填/切读方案、只读验证步骤、人工审批和暂停条件。

### 8.11 网络隔离与安全域

**K8s NetworkPolicy 设计**：

```
┌─────────────────────────────────────────────────────────────────────┐
│                         安全域划分                                    │
│                                                                      │
│  ┌───────────────────── dmz 区 (Ingress) ──────────────────────┐    │
│  │  nginx (可被内网所有用户访问)                                  │    │
│  │  Ingress: 0.0.0.0/0:443                                     │    │
│  └─────────────────────┬───────────────────────────────────────┘    │
│                        │                                            │
│  ┌───────────────────── app 区 ─────────────────────────────────┐   │
│  │  api-pod (仅 nginx → api:8000)                               │   │
│  │  worker-pods (无 Ingress，仅出站)                              │   │
│  │  Egress: pg-svc:5432, redis-svc:6379, rabbitmq-svc:5672,     │   │
│  │          es-svc:9200, minio-svc:9000                         │   │
│  └─────────────────────┬───────────────────────────────────────┘    │
│                        │                                            │
│  ┌───────────────────── data 区 ─────────────────────────────────┐   │
│  │  pg-svc (仅 app 区 → :5432)                                  │   │
│  │  redis-svc (仅 app 区 → :6379)                               │   │
│  │  rabbitmq-svc (仅 app 区 → :5672)                            │   │
│  │  es-svc (仅 app 区 → :9200)                                  │   │
│  │  minio-svc (仅 app 区 → :9000)                               │   │
│  │  Egress: NAS/NTP/DNS only                                    │   │
│  └──────────────────────────────────────────────────────────────┘    │
│                                                                      │
│  默认策略: 拒绝所有跨 Pod 通信，仅开放明确声明的规则                   │
│  外部系统 (风控/OA/MDM): 通过专用 VPN/专线 + IP 白名单接入            │
└─────────────────────────────────────────────────────────────────────┘
```

**密钥管理方案**：

| 密钥类型 | 存储方式 | 轮换周期 | 访问控制 |
|----------|----------|----------|----------|
| DB 密码 / API Key | Vault/External Secrets → K8s Secrets (etcd 加密) | 90 天 | RBAC + Namespace 隔离 |
| JWT 签名密钥 | Vault/External Secrets → K8s Secrets | 30 天 (滚动更新，支持多密钥验证) | 仅 api-pod ServiceAccount |
| AES 加密密钥 | Vault/External Secrets → 独立 K8s Secret | 180 天 | 仅 api-pod + worker-pod |
| TLS 证书 | cert-manager (自动签发+续期) | 90 天 (自动续期) | Ingress Controller |

> 等保二级要求密钥与业务数据分离存储。生产默认采用 Vault 或 External Secrets；非生产本地/测试可使用本地 Secret 文件，但必须限制权限，且不得复用生产密钥。

### 8.12 API 版本管理策略

| 策略 | 说明 |
|------|------|
| **版本格式** | URL 路径版本: `/api/v1`、`/api/v2` |
| **兼容性承诺** | 同一主版本内向后兼容 (不删除字段、不修改字段类型)；新增字段可接受 |
| **弃用流程** | ① 新版 API 上线 → ② 旧版标记 `Deprecated` (响应头 + 文档) → ③ 6 个月过渡期 (监控旧版调用量) → ④ 调用方迁移完毕 → ⑤ 旧版下线 |
| **破坏性变更** | 仅在新主版本引入 (如 `/api/v2`)；旧版本并行运行至少 6 个月 |
| **文档同步** | OpenAPI 3.0 自动生成 (FastAPI 内置)，Swagger UI 实时查看；版本间差异用 `@deprecated` 标记 |

### 8.13 多租户数据隔离方案

系统涉及三个法人实体（科沃斯、添可、集团），需在共享基础设施上实现数据隔离：

| 维度 | 隔离方式 | 实现 |
|------|----------|------|
| **数据库层** | 共享数据库 + PG RLS 行级隔离 | 所有租户相关表包含 `client` 字段 (`ecovacs` / `tineco` / `group`)；P1 生产默认启用 Row-Level Security |
| **应用层** | RBAC 三层角色 + 查询自动注入 | API 中间件自动注入 `WHERE client = $current_user_role`；`group` 角色仅在显式全局权限下跳过过滤 |
| **文件存储** | MinIO Bucket 按 client 分区 | `hermes-ecovacs` / `hermes-tineco` / `hermes-group` 三个独立 Bucket |
| **搜索索引** | ES 索引按 client 别名 | `hermes-docs-ecovacs` / `hermes-docs-tineco` / `hermes-docs-group`；搜索时自动路由 |
| **缓存** | Redis key 前缀隔离 | `ecovacs:cache:...` / `tineco:cache:...` / `group:cache:...` |
| **审计** | 审计日志记录 client 上下文 | 每条日志标记 `client` 字段，支持按租户独立审计 |

> 隔离原则：科沃斯用户不可查询添可数据，反之亦然。集团用户拥有全局视角但必须有显式全局权限。生产使用应用层过滤 + PG RLS 双保险，禁止只依赖应用层拦截。

### 8.14 智能体记忆架构设计

记忆是 AI 智能体区别于传统规则系统的核心能力。赫尔墨斯的记忆体系采用**分层记忆架构**，覆盖从单次交互到跨案件学习的完整记忆谱系。

#### 8.14.1 记忆分层模型

```
┌─────────────────────────────────────────────────────────────────┐
│                    赫尔墨斯记忆分层架构                             │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  L4: 组织记忆 (Organizational Memory)                        │ │
│  │  跨案件/跨用户/跨模块的持久知识                                │ │
│  │  ├── 知识库 (RAG): 制度法规、历史案例、报告模板、风险规则      │ │
│  │  ├── 反馈闭环: 处置结果回流 → 规则优化 → 知识更新             │ │
│  │  └── 审计日志: 全量操作记录，支撑复盘与模式发现               │ │
│  │  存储: Milvus + Elasticsearch/OpenSearch + PostgreSQL metadata│ │
│  │  生命周期: 永久 (持续更新)                                    │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                              ↑ 固化                               │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  L3: 案例记忆 (Episodic Memory)                              │ │
│  │  单个案件的完整处理轨迹与决策链                                │ │
│  │  ├── 案件工作流状态 (LangGraph State + Checkpointer)          │ │
│  │  ├── 阶段产出物 (初判报告/调查方案/分析报告/追责意见)          │ │
│  │  ├── 碳基守门记录 (审批决策/修改内容/驳回原因)                 │ │
│  │  └── 外部交互记录 (A2A 任务/风控同步/法务回传)                │ │
│  │  存储: Redis (活跃) + PostgreSQL (持久)                       │ │
│  │  生命周期: 案件活跃期 → 温层归档 (90天) → 冷层 (2年+)        │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                              ↑ 聚合                               │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  L2: 会话记忆 (Conversational Memory)                        │ │
│  │  单个工作流阶段内的多轮人机交互上下文                           │ │
│  │  ├── LLM 对话历史 (messages: BaseMessage[])                  │ │
│  │  ├── 检索上下文 (本轮 RAG 召回的文档片段)                      │ │
│  │  ├── Tool 调用结果缓存 (避免重复计算)                         │ │
│  │  └── Context Window 管理 (摘要压缩 + 滑动窗口)                │ │
│  │  存储: PostgreSQL durable checkpoint + Redis 热缓存            │ │
│  │  生命周期: 单阶段 (守门完成后归档 → 压缩摘要保留)              │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                              ↑ 交互                               │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  L1: 感知记忆 (Sensory Memory)                               │ │
│  │  单次 LLM 调用的即时输入                                       │ │
│  │  ├── 用户输入 (文本/字段/附件引用)                             │ │
│  │  ├── 多模态感知结果 (ASR 转录/OCR 文本/视频描述)               │ │
│  │  └── 系统注入 (System Prompt + 阶段知识 + 历史摘要)           │ │
│  │  存储: LLM Context Window (瞬时, 无持久化)                    │ │
│  │  生命周期: 单次 LLM 调用结束即释放                             │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

#### 8.14.2 记忆流转与生命周期

```
用户操作 → L1 感知记忆 ──LLM推理──→ L2 会话记忆 (多轮对话)
                                        │
                        ┌───────────────┘ 守门通过
                        ↓
                  L3 案例记忆 (阶段产物 + 决策链)
                        │
            ┌───────────┼───────────┐
            ↓           ↓           ↓
       后续阶段引用   问题汇入     相似案件推荐
       (继续调查)   持续改善模块   (风险监控回流)
                        │
                        ↓ 案件闭环
                  ┌─────────────────┐
                  │   L3 → L4 固化   │
                  │                  │
                  │  调查报告 → 案例库│
                  │  处置经验 → 规则库│
                  │  缺陷模式 → 指标库│
                  └─────────────────┘
```

#### 8.14.3 记忆检索策略

| 记忆类型 | 检索方式 | 触发时机 | 技术实现 |
|----------|----------|----------|----------|
| **L4 组织记忆** | 向量语义检索 + 全文关键词 + 结构化过滤 | 每个工作流阶段启动时 | Milvus 向量召回 + Search Adapter (BM25) + SQL 元数据过滤；混合检索 + Re-ranking |
| **L3 案例记忆** | 结构化查询 + 相似案例检索 | 调查方案生成 / 案件定性 / 追责建议时 | SQL 查询历史案件 (同类型/同部门/同金额区间)；Milvus 相似案例召回 |
| **L2 会话记忆** | LangGraph State 直接读取 | 单阶段内后续 LLM 调用 | `state["messages"]` 自动注入；超长对话自动摘要压缩 (LLM 生成 200 token 摘要) |
| **L1 感知记忆** | 实时注入 | 每次 LLM 调用 | System Prompt 模板 + 阶段知识注入 + RAG 上下文组装 |

#### 8.14.4 上下文窗口管理策略

针对 LLM 上下文窗口有限（DeepSeek 64K tokens）的问题：

| 策略 | 触发条件 | 处理方式 |
|------|----------|----------|
| **滑动窗口** | messages 总 token > 32K | 保留最近 16K tokens + System Prompt + 早期摘要 |
| **自动摘要** | 单阶段对话超过 10 轮 | LLM 生成 200 token 结构化摘要（关键事实/决策/待办），替换早期详细对话 |
| **分层注入** | 每阶段启动 | System Prompt (固定) → 阶段知识 (RAG top 5, ~2K tokens) → 历史摘要 (~500 tokens) → 用户输入 |
| **引用而非复制** | 引用大段文档内容 | 仅保留文档 ID + 相关片段引用，完整文档按需再次检索 |

#### 8.14.5 跨案件学习闭环

```
案件 A (已完成) ──→ 案例记忆 (L3)
                        │
            ┌───────────┼───────────┐
            ↓           ↓           ↓
       调查报告入库   处罚经验提取   缺陷模式识别
       (案例库更新)   (规则库更新)   (指标库更新)
            │           │           │
            └───────────┼───────────┘
                        ↓
              组织记忆 (L4) 更新
                        │
            ┌───────────┼───────────┐
            ↓           ↓           ↓
       案件 B         风险监控      内控评价
       (调查方案     (规则命中率    (缺陷识别
        自动引用      提升)         更精准)
        先例)
```

**固化机制**：
- **自动固化**：案件闭环后，调查报告 + 关键决策 + 处置结果自动入库（结构化特征提取 + Embedding 索引）
- **人工审核**：高风险案件（涉及刑事/重大金额）需人工审核后入库
- **衰减/淘汰**：过时案例（法律法规已变更）标记为"仅供参考"，权重降低
- **指标迭代**：风险监控模块的处置结果回流 → 误报规则优化 → 准确率持续提升（已体现在 风险监控 [6.6] 阶段）

### 8.15 等保二级合规映射表

| 等保二级控制项 | 对应架构设计 | 文档引用 |
|--------------|-------------|----------|
| **身份鉴别 (L2-IA)** | 企业 SSO/OIDC/AD + JWT 令牌 8h 过期 + Refresh 7天；失败锁定 30 分钟；关键操作 MFA | 8.1 安全性设计 |
| **访问控制 (L2-AC)** | RBAC 三级角色 (集团/科沃斯/添可)；应用层过滤 + PG RLS；字段级权限 | 8.1 / 8.13 多租户 |
| **安全审计 (L2-SA)** | 全量操作日志 → audit_log → ES 索引 1 年；不可删除/篡改 | 8.1 / 8.5 可观测性 |
| **通信保密性 (L2-CP)** | TLS 1.3 终结 (Nginx)；内网 mTLS；K8s NetworkPolicy 分区隔离 | 8.1 / 8.11 网络隔离 |
| **数据保密性 (L2-DC)** | AES-256-GCM 列级加密；MinIO SSE-S3；密钥 Vault/External Secrets 管理 | 8.1 / 8.11 密钥管理 |
| **软件容错 (L2-SF)** | 关键节点重试 3 次 → 人工接管；RabbitMQ quorum queues + DLX；Celery 自动重试 | 8.1 / 8.4 可用性 |
| **资源控制 (L2-RC)** | 令牌桶限流 (全局 1000/s + 用户 100/min + LLM 50/s)；上传 500MB；PgBouncer pool_size=500 | 8.1 |
| **数据备份与恢复 (L2-DB)** | PG 每日全量 + WAL 归档 (MinIO + NAS 双副本)；Redis AOF+RDB；灾备季度演练 | 8.1 / 8.6 BCP |
| **剩余信息保护 (L2-RP)** | JWT 黑名单全局同步；登出立即失效；structlog 敏感字段脱敏 | 8.1 |

### 8.16 SLO/SLI 定义与容量规划

#### SLO 指标定义

| 服务 | SLI | SLO | 测量窗口 | 告警阈值 |
|------|-----|-----|----------|----------|
| **API 可用性** | 成功请求数 / 总请求数 (2xx/3xx) | ≥ 99.9% | 30 天 | < 99.5% → P1 |
| **API 延迟** | P95 响应时间 (不含 LLM 调用) | < 500ms | 7 天 | > 1s → P1 |
| **LLM 可用性** | 成功调用数 / 总调用数 | ≥ 99.5% | 7 天 | < 98% → P1 (切换备用) |
| **LLM 延迟** | P95 端到端推理时间 | < 15s | 7 天 | > 20s → P2 |
| **知识库检索** | P95 检索延迟 | < 200ms | 7 天 | > 500ms → P2 |
| **多模态处理** | 任务成功率 (完成/提交) | ≥ 99% | 7 天 | < 95% → P2 |
| **消息队列** | 消息积压 (队列深度) | < 500 | 实时 | > 1000 → P3 |
| **数据迁移** | 热→温迁移成功率 | ≥ 99.9% | 30 天 | < 99% → P2 |

**错误预算**：API 99.9% = 月停机 ≤ 43 分钟。消耗 > 80% 时冻结生产变更，优先稳定性修复。

#### 容量规划触发条件

| 资源 | 扩容阈值 | 缩容阈值 | 最大容量 | 扩容前置时间 |
|------|----------|----------|----------|-------------|
| **API Pods** | CPU > 70% 持续 5min 或 内存 > 80% | CPU < 30% 持续 10min | 20 pods | 即时 (HPA) |
| **Worker Pods (各类)** | 队列深度 > 阈值的 80% 持续 10min | 队列深度 < 10% 持续 30min | 各 1-5 pods（按池类型不同） | 即时 (KEDA) |
| **PG 存储** | 使用率 > 70% | — | 2TB (SSD) | 提前 2 周规划扩容 |
| **PG 连接数** | PgBouncer 活跃连接 > 400 | — | pool_size=500 | 即时 (调整参数) |
| **MinIO 存储** | 使用率 > 70% (~14TB) | — | ~20TB (可用) | 提前 1 月加节点 |
| **ES 存储** | 使用率 > 75% | — | 6TB (3×2TB) | 提前 2 周加节点 |
| **Redis 内存** | 使用率 > 70% | — | 16GB/节点 | 提前 1 周加节点 |
| **NAS 冷存储** | 使用率 > 80% | — | 50TB | 提前 2 月采购扩容 |

### 8.17 生产变更管理

```
┌─────────────────────────────────────────────────────────────────┐
│                      生产变更审批流程                               │
│                                                                   │
│  变更发起 (MR → main)                                              │
│       │                                                           │
│       ├── 低风险变更 (配置/文档/索引)                                │
│       │   → Tech Lead 审批 → 自动部署                              │
│       │                                                           │
│       ├── 中风险变更 (代码逻辑/新增 API/Worker 调整)                  │
│       │   → Tech Lead + 1 Reviewer → Staging 验证 → 金丝雀部署      │
│       │                                                           │
│       └── 高风险变更 (DB Schema/基础设施/LLM Prompt 版本)            │
│           → 变更委员会审批 (Tech Lead + 风控负责人 + DBA)            │
│           → Staging 全量回归 → 维护窗口 (凌晨 2-4 点)                │
│           → 金丝雀 10% (30min) → 50% (1h) → 100%                  │
│           → 监控 2h 无异常 → 变更关闭                               │
│                                                                   │
│  回滚条件: 错误率 > 基线 2x / P95 延迟 > 基线 2x / 用户投诉         │
│  回滚目标: < 5 分钟 (kubectl rollout undo)                         │
│                                                                   │
│  变更窗口: 每周二/四 14:00-16:00 (常规)；紧急变更 7×24 (需总监审批)  │
│  变更冻结: 月末结账期 (最后 3 个工作日) / 法定节假日前 1 天           │
└─────────────────────────────────────────────────────────────────┘
```

### 8.18 成本估算

> **说明**：以下成本估算仅作为参考量级，实际价格受采购渠道、硬件品牌、云服务商定价策略等因素影响，应以实际报价为准。

#### 8.18.1 P1 生产架构成本（10K 注册用户 / 10TB，K8s 集群）

**一次性硬件投入**：

| 资源类型 | 数量 | 配置 | 单价估算 | 小计 |
|----------|------|------|----------|------|
| K8s Master 节点 | 3 | 4C/8G/100GB SSD | ¥8,000-15,000 | ¥24,000-45,000 |
| K8s Worker 节点 (CPU) | 5 | 16C/32G/200GB SSD | ¥15,000-25,000 | ¥75,000-125,000 |
| K8s Worker 节点 (GPU V100) | 2 | 16C/64G/200GB SSD + V100 32G | ¥60,000-100,000 | ¥120,000-200,000 |
| PostgreSQL 主机 | 3 | 8C/32G/2TB SSD | ¥25,000-40,000 | ¥75,000-120,000 |
| Elasticsearch/OpenSearch 主机 | 3 | 8C/32G/1TB SSD | ¥20,000-35,000 | ¥60,000-105,000 |
| Milvus 向量检索节点 | 3 | 8C/32G/1TB SSD | ¥20,000-35,000 | ¥60,000-105,000 |
| MinIO 存储节点 | 4 | 4TB HDD | ¥10,000-15,000 | ¥40,000-60,000 |
| NAS 冷归档 | 1 | 50TB | ¥30,000-50,000 | ¥30,000-50,000 |
| 网络设备 (交换机/防火墙) | — | — | ¥20,000-50,000 | ¥20,000-50,000 |
| **硬件合计** | | | | **¥504,000-860,000** |

**月度运营费用**：

| 项目 | 月度估算 |
|------|----------|
| IDC 托管 (8-10U) | ¥5,000-15,000 |
| Model Gateway Provider 成本 (DeepSeek/Qwen/私有 endpoint) | ¥20,000-60,000 |
| Embedding API | ¥3,000-8,000 |
| SSL 证书 / 域名 | ¥0-500 |
| **月度合计** | **¥28,500-75,500** |
| **年度合计** | **¥342,000-906,000** |

**人力成本（年）**：

| 角色 | 人数 | 说明 |
|------|------|------|
| SRE/DevOps 工程师 | 2-3 | K8s 集群运维、监控、CI/CD |
| 后端开发 | 3-5 | FastAPI/LangGraph/Celery |
| 前端开发 | 1-2 | Vue 3 SPA |
| AI/ML 工程师 | 1-2 | LLM/RAG/多模态调优 |
| **人力合计** | **7-12 人** | — |

**首年 TCO（含硬件 + 运营 + 人力）**：约 ¥3,000,000-6,000,000

---

#### 8.18.2 生产成本控制建议

| 措施 | 节省 | 说明 |
|------|------|------|
| **自托管 Embedding 模型** | API 成本下降 | 使用 `bge-large-zh-v1.5` 等中文模型，纳入 Model Gateway 和统一评测 |
| **私有 LLM endpoint** | 长期推理成本下降 | 对高频、低敏或固定格式任务使用私有模型 endpoint，仍由 Model Gateway 路由 |
| **检索冷热分层** | 搜索/向量存储成本下降 | 活跃知识集合使用高性能索引，归档知识集合降副本或冷存 |
| **对象存储生命周期策略** | MinIO/NAS 成本下降 | 关闭案件大文件按生命周期转温/冷层，保留可搜索元数据 |
| **使用 Let's Encrypt 免费 SSL** | ¥0 | 自动化续期，无需付费证书 |

---

### 8.19 知识库初始化策略

#### 8.19.1 冷启动风险

RAG 系统的质量高度依赖知识库的丰富程度。一个空的知识库上线后，LLM 缺乏上下文支撑，将出现以下问题：

- **初判准确率低**：没有历史案例参考，LLM 倾向给出通用判断
- **调查方案质量差**：无法匹配类似案件的成功调查路径
- **法规引用不可靠**：没有制度法规注入，LLM 可能"幻觉"出不存在的规定
- **用户体验差**：首次使用即遭遇低质量输出，用户信任难以建立

#### 8.19.2 上线准备步骤与数据源

**步骤 0：上线前 2 个月 — 基础数据采集**

| 数据类型 | 来源 | 数量估算 | 优先级 | 负责人 |
|----------|------|----------|--------|--------|
| 集团制度文件 | OA 系统/制度管理平台 | 100-500 份 | P0 必须 | 风控部门 |
| 组织架构信息 | HR 系统/MDM | 1 份完整树 | P0 必须 | IT 部门 |
| 法律法规库 | 威科先行/司法网站 | 50-200 份 | P0 必须 | 法务部门 |
| 历史案件数据 | 风控系统导出 | 已有案件全量 | P0 必须 | 风控部门 |
| 历史审计报告 | 审计部门 | 20-50 份 | P1 推荐 | 审计部门 |
| 投诉举报历史 | 风控系统 | 50-200 条 | P1 推荐 | 风控部门 |
| 内控矩阵 | 内控评价历史 | 按 19 个循环 | P1 推荐 | 内控部门 |
| 供应商黑名单 | MDM 系统 | 已有全量 | P1 推荐 | 采购部门 |
| 侵权案例库 | 法务部门/威科先行 | 50-100 份 | P2 可选 | 法务部门 |
| 处罚公告历史 | OA 系统 | 50-100 份 | P2 可选 | HR 部门 |

**步骤 1：上线前 1 个月 — 数据处理与入库**

```
原始文件
  │
  ├── 文档解析 (unstructured.io)
  │   ├── PDF/Word → 文本提取 + 表格抽取 + 层次结构识别
  │   └── 人工校验关键文档（制度法规、案件结论）
  │
  ├── 智能分块 (Chunking)
  │   ├── 按语义边界分块（512-2048 tokens/chunk）
  │   ├── 保留层级上下文（章节标题+小节标题+段落）
  │   └── 元数据标注（来源、类型、日期、部门、密级）
  │
  ├── 向量化 (Embedding)
  │   ├── text-embedding-3-large → 1536d 向量
  │   └── 或 bge-large-zh-v1.5 → 1024d 向量（本地部署）
  │
  └── 索引
      ├── Milvus: collection 按模块/租户隔离 + 向量索引
      ├── Elasticsearch/OpenSearch: 全文索引 + 中文分词
      └── PostgreSQL: 结构化元数据（文档名/类型/日期/状态）
```

**步骤 2：上线前 2 周 — 质量验证**

| 验证项 | 方法 | 合格标准 |
|--------|------|----------|
| **检索覆盖率** | 准备 50 个典型查询，检查是否能召回相关内容 | > 80% 查询返回相关结果 |
| **检索准确率** | 人工评估 Top 5 结果的相关性 | > 70% 结果相关 |
| **法规引用准确性** | 抽查 LLM 引用的法规条款 | 引用内容与原文一致 |
| **案例相似度** | 输入已知案例的摘要，检索类似案例 | Top 3 中包含相同类型案件 |
| **知识库完整性** | 按模块×阶段矩阵检查覆盖 | 每个已实现模块至少有 20+ 相关文档 |

#### 8.19.3 上线后持续更新

| 更新类型 | 频率 | 触发条件 | 方式 |
|----------|------|----------|------|
| **制度法规更新** | 按需 | 新制度发布/旧制度废止 | 人工上传 + 自动标记旧版为"已废止" |
| **案件经验入库** | 自动（闭环触发） | 案件关闭 + 守门确认 | 自动抽取关键信息 → Embedding → 入库 |
| **规则库迭代** | 每月 | 风险监控误报率统计 | 人工审核 + 参数调整 |
| **知识库清理** | 每季度 | — | 删除过时内容（法规废止 > 1 年），降级低质量文档 |

#### 8.19.4 知识库覆盖率 KPI

| 模块 | 上线前最低文档数 | 上线后 3 月目标 | 上线后 6 月目标 |
|------|-----------------|---------------|---------------|
| 廉洁监察 | 50+ (制度+历史案例) | 100+ | 200+ |
| 风险监控 | 30+ (风险规则+场景) | 60+ | 100+ |
| 内控评价 | 30+ (19 循环矩阵) | 80+ | 150+ |
| 专项审计 | 20+ (审计方案模板) | 50+ | 100+ |
| 离任审计 | 20+ (审计方案模板) | 40+ | 80+ |
| 商业秘密 | 30+ (法规+案例+制度) | 80+ | 150+ |
| 行为风险 | 15+ (法规+行为规则) | 40+ | 80+ |
| 持续改善 | 15+ (整改模板+历史) | 40+ | 80+ |
| 公共知识库 | 50+ (通用制度+组织架构) | 100+ | 150+ |

---

### 8.20 应急预案

#### 8.20.1 应急响应等级

| 等级 | 定义 | 响应时间 | 通知范围 | 处理人 |
|------|------|----------|----------|--------|
| **P0 紧急** | 核心功能完全不可用（API 全宕/DB 损坏/数据泄露） | < 15min | 全员通知 | 技术负责人 + 风控负责人 |
| **P1 严重** | 部分功能不可用或性能严重下降（LLM 不可用/搜索不可用/用户无法登录） | < 30min | 受影响用户群 | 技术负责人 |
| **P2 警告** | 功能降级可用（外部系统不可用/队列积压/冷存储满） | < 2h | 开发团队 | 开发值班 |
| **P3 提醒** | 非紧急异常（告警规则触发/磁盘使用 > 70%/过期备份） | < 1 天 | 无 | 开发值班 |

#### 8.20.2 场景化应急 SOP

**SOP-01: LLM Provider 不可用**

```
现象: 单个 LLM provider 持续返回 5xx 或超时（> 30s），持续 5 分钟以上
影响: 该 provider 承载的 AI 功能延迟升高或失败

处理步骤:
1. [5min]  确认故障范围：检查 Model Gateway provider health、错误率、超时率和外部状态页
2. [自动]  Model Gateway 熔断故障 provider，将流量切到备用 provider 或私有 endpoint
3. [5min]  验证：调用 /api/v1/health/llm 确认 routing policy 与备用 provider 可用
4. [持续]  监控：关注备用 provider 成功率、P95 延迟、Token 成本和输出质量漂移
5. [恢复]  故障 provider 恢复后，观察 30 分钟稳定，通过灰度路由逐步切回
6. [事后]  复盘报告：故障时长、影响范围、改进措施

降级行为: 用户操作不受影响（数据查看、守门审批等非 AI 功能正常）；全部 provider 不可用时，workflow 进入 human_intervention
恢复目标: < 5min（自动切换）/ < 15min（手动切换）
```

**SOP-02: 数据泄露事件**

```
现象: 发现未经授权的数据访问或数据外传
影响: 合规风险，可能触发等保违规

处理步骤:
1. [立即]  隔离：断开受影响服务的外部网络连接
   iptables -A OUTPUT -d <可疑IP> -j DROP
2. [15min] 取证：保留所有访问日志（audit_log、nginx access log、PG 日志）
3. [30min] 排查：确定泄露范围（哪些用户/哪些数据/泄露途径）
4. [2h]   通知：向风控负责人报告；如涉及个人信息，准备通报监管
5. [4h]   修复：修补漏洞（代码修复/配置加固/权限收缩）
6. [24h]  审计：全量审计日志审查，确认无其他泄露
7. [事后] 改进：更新安全策略，加强访问控制

注意: 不要删除任何日志或数据，保留完整证据链
```

**SOP-03: 数据库主库损坏**

```
现象: PostgreSQL 无法启动或数据文件损坏，无法恢复
影响: 所有读写操作不可用

生产 K8s 处理步骤:
1. [5min]  确认 Patroni 状态、PgBouncer 路由和 replica 延迟，禁止直接执行破坏性 `pg_resetwal`
2. [15min] 由 Patroni 提升健康 Replica 为 Primary，PgBouncer write pool 切换到新主库
3. [15min] 验证：核心 API 健康检查 + 10 个冒烟测试 + 写入/读取一致性检查
4. [2h]   如整个 PG 集群不可用，使用 pgBackRest + WAL 归档执行 PITR 恢复
5. [事后] 修复旧主库，按新副本方式重新加入 Patroni 集群

非生产 Docker Compose 本地/测试处理步骤:
1. [10min] 确认主库无法恢复后，停止写入并保留现场日志
2. [30min] 如有备份主机，提升备份库或切换 DB_HOST；否则从最近定时备份恢复
3. [30min] 验证核心 API 和登录/案件查询/写入

恢复目标:
- P1 主库故障: RPO < 5min / RTO < 15min
- P1 数据库集群全故障: RPO < 1min / RTO < 2h
- 非生产本地/测试环境不承诺生产 RPO/RTO；每日备份场景下恢复演练参考值为 RPO < 24h / RTO < 2h
```

**SOP-04: Elasticsearch 不可用**

```
现象: ES 查询返回错误或超时，搜索功能不可用
影响: 全文检索不可用，知识库检索仅能用向量检索

处理步骤:
1. [5min]  确认：检查 ES 容器状态和日志
   docker compose logs elasticsearch --tail 100
2. [10min] 尝试恢复：
   docker compose restart elasticsearch
3. [15min] 如无法恢复，启用降级：
   - 应用自动检测 ES 不可用 → 跳过全文检索
   - 知识库检索降级为 Milvus 向量检索 + PostgreSQL 元数据过滤
   - 审计日志搜索降级为 PG 查询（速度慢但可用）
4. [恢复]  ES 恢复后，重新启用全文检索
5. [事后] 如频繁发生，升级为单节点集群（加副本）

降级行为: 搜索功能降级但可用（仅向量检索，缺少关键词匹配）
```

**SOP-05: 主机磁盘空间不足**

```
现象: 磁盘使用 > 85%，Prometheus 告警触发
影响: 可能导致数据库写入失败（磁盘满）

处理步骤:
1. [10min] 紧急释放空间：
   - 清理 Docker 日志: docker system prune -f
   - 清理旧备份: find /opt/hermes/backup/pg/ -mtime +7 -delete
   - 清理 ES 旧快照
2. [30min] 排查原因：
   - du -sh /opt/hermes/data/* | sort -rh | head -10
3. [2h]   如无法释放足够空间：
   - 加速热→温数据迁移（案件关闭 > 30 天立即迁移）
4. [长期] 扩容磁盘或增加数据生命周期频率
```

#### 8.20.3 应急联系人模板

| 角色 | 姓名 | 手机 | 邮箱 | 值班安排 |
|------|------|------|------|----------|
| 技术负责人 (P0) | — | — | — | 7×24 待命 |
| 后端开发 (P1) | — | — | — | 轮班 |
| 风控负责人 (P0/P1) | — | — | — | 工作时间 |
| IT 基础设施 (P1) | — | — | — | 工作时间 |

#### 8.20.4 应急演练计划

| 演练场景 | 频率 | 方式 | 参与人 |
|----------|------|------|--------|
| LLM 切换演练 | 每月 | 主动切换 DeepSeek → 通义千问 | 后端 + AI |
| 数据库恢复演练 | 每季度 | 从备份恢复至测试环境 | 后端 |
| 冷备切换演练 | 每半年 | 手动切到冷备机，运行 1 天 | 后端 + IT |
| 数据泄露应急桌面演练 | 每年 | 模拟场景讨论 | 全体 |
| 完整灾备演练 | 每年 | 冷备机完整接管 1 天 | 全体 |

---

### 8.21 开发与测试环境方案

#### 8.21.1 环境矩阵

| 环境 | 用途 | 配置 | 数据 | 部署方式 |
|------|------|------|------|----------|
| **local** | 本地开发 | 开发者笔记本，Docker Compose（仅 PG + Redis） | 最小数据集（fixture） | `docker compose -f docker-compose.dev.yml` |
| **dev** | 联调测试 | 低配主机 (8C/16G/500GB)，所有服务 | 脱敏测试数据 | CI 自动部署（分支 push） |
| **staging** | 预发布验证 | 与生产同配或降配，完整服务 | 脱敏生产数据子集 | CI 自动部署（develop 分支） |
| **production** | 生产环境 | 见部署方案 | 真实数据 | 手动触发 + 金丝雀 |

#### 8.21.2 Docker Compose 开发环境

```yaml
# docker-compose.dev.yml — 开发环境（仅基础设施，应用在本地运行）
version: "3.9"

services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: hermes_dev
      POSTGRES_USER: hermes
      POSTGRES_PASSWORD: dev_password
    ports:
      - "5432:5432"
    volumes:
      - pg_dev_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.17.0
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
      - xpack.security.enabled=false
    ports:
      - "9200:9200"

  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: hermes
      RABBITMQ_DEFAULT_PASS: dev_password
    ports:
      - "5672:5672"
      - "15672:15672"

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"

volumes:
  pg_dev_data:
```

#### 8.21.3 测试数据管理

| 类型 | 数据来源 | 安全措施 |
|------|----------|----------|
| **本地开发** | Pydantic fixture 工厂 + Faker 生成 | 无敏感数据 |
| **dev 环境** | 手动编写的测试用例数据 | 无敏感数据 |
| **staging 环境** | 生产数据脱敏子集（约 1%） | AES-256-GCM 解密后伪匿名化处理 |
| **E2E 测试** | Playwright fixture + 脱敏数据 | 同 staging |
| **性能测试** | 数据生成器模拟写入负载 | 无敏感数据 |

**脱敏规则**（staging/E2E 环境）：

| 原始数据 | 脱敏方式 | 示例 |
|----------|----------|------|
| 姓名 | 伪随机替换（中文姓名库） | "张三" → "李建国" |
| 手机号 | 保留前 3 后 2，中间替换 | "13812345678" → "138*****678" |
| 邮箱 | 域名保留，用户名哈希 | "zhangsan@ecovacs.com" → "a1b2c3@ecovacs.com" |
| 身份证号 | 随机生成符合校验的假号 | — |
| 金额 | 等比例缩放（×随机系数 0.5-2.0） | ¥100,000 → ¥87,342 |
| 供应商名称 | 替换为"供应商001"等 | "XX科技有限公司" → "供应商042" |
| 附件/文件 | 替换为同类型的样本文件 | — |

#### 8.21.4 CI/CD 开发环境部署

```yaml
# .gitlab-ci.yml 中 dev 环境部分
deploy:dev:
  stage: deploy
  only:
    - feature/*
  script:
    - ssh dev-server "cd /opt/hermes-dev && docker compose pull && docker compose up -d"
    - ssh dev-server "docker compose exec -T api alembic upgrade head"
  environment:
    name: dev
```

---

### 8.22 模型版本管理策略

#### 8.22.1 涉及的 AI 模型

| 模型 | 用途 | 升级影响 | 风险等级 |
|------|------|----------|----------|
| **Embedding 模型** | 文档/查询向量化 | 向量维度/语义空间变化 → **全量数据需重新向量化** | 🔴 高 |
| **LLM (DeepSeek)** | 推理/生成 | Prompt 行为可能变化 → 输出质量波动 | 🟡 中 |
| **Whisper (ASR)** | 音频转文字 | 转录结果格式可能变化 | 🟢 低 |
| **PaddleOCR** | 图像文字提取 | OCR 结果文本可能不同 | 🟢 低 |
| **CLIP/ViT** | 图像分类 | 分类标签体系可能变化 | 🟢 低 |
| **unstructured.io** | 文档解析 | Chunk 切分策略变化 → 需重新 Chunking | 🟡 中 |
| **IK 分词器** | ES 中文分词 | 词典变化 → 需重建 ES 索引 | 🟡 中 |

#### 8.22.2 Embedding 模型升级流程（最高风险）

```
当前: text-embedding-3-large (1536d) → 未来可能升级为 text-embedding-4 或本地 bge 模型

升级步骤:
1. [准备阶段，1 周]
   ├── 搭建新向量表 (kb_chunks_v2)，使用新模型维度
   ├── 双写机制：新文档同时写入两个向量表
   └── 混合检索适配器：兼容查询新旧两个向量表

2. [迁移阶段，1-2 周]
   ├── Celery 异步任务批量向量化 (100 chunks/批次)
   ├── 后台执行，不影响正常业务
   └── 进度监控：已迁移 / 总量

3. [切换阶段，1 天]
   ├── Staging 环境验证：检索质量对比 + Re-ranking 准确性
   ├── 选择低峰期（周末）切换
   └── 回滚预案：保留旧向量表至少 30 天

4. [清理阶段，30 天后]
   └── 确认检索质量无退化 → 删除旧向量表
```

#### 8.22.3 LLM 版本升级流程

```
Provider 模型升级（例如 DeepSeek 或 Qwen 的一个具体模型版本升级）:

1. [评估阶段]
   ├── 准备 50 个标准测试 Prompt（覆盖 8 个模块的关键场景）
   ├── 新旧模型分别运行，人工对比输出质量
   └── 关注：输出格式一致性、推理逻辑准确性、中文表达质量

2. [灰度阶段]
   ├── Model Gateway 按 task_id 哈希将 10% 流量切换到新 provider/model_version
   ├── LangFuse/Prometheus 监控：成功率、延迟、Token 消耗
   └── 持续 3 天无异常 → 扩大灰度

3. [全量切换]
   └── 100% 切换到新 provider/model_version，旧版本保留为可回滚路由

回滚条件: 成功率 < 98% 或 P95 延迟 > 基线 2x 或人工评估质量下降
```

#### 8.22.4 模型版本矩阵

| 模型/Provider | 当前版本 | 锁定策略 | 升级评估周期 |
|---------------|----------|----------|-------------|
| text-embedding-3-large | 1536d | API 固定版本号 | 每季度评估 |
| DeepSeek provider | 由 Model Registry 记录具体 model_version | Provider + model_version + routing policy 固定 | 每季度评估 |
| Qwen provider | 由 Model Registry 记录具体 model_version | Provider + model_version + routing policy 固定 | 每季度评估 |
| 私有模型 endpoint | 由 Model Registry 记录镜像 digest 和权重版本 | 镜像 digest + 权重 checksum 固定 | 每季度评估 |
| Whisper | large-v3 | Docker 镜像 Tag | 每半年评估 |
| PaddleOCR | latest stable | Docker 镜像 Digest | 按需 |
| CLIP | ViT-B/32 | 模型权重文件 MD5 | 按需 |
| unstructured | 0.15+ | pip 版本锁定 | 每月评估 |
| IK Analyzer | 8.17.0 | 下载版本固定 | 跟随 ES 升级 |

#### 8.22.5 AI 治理与发布门禁

| 风险 | 控制措施 | 发布门禁 |
|------|----------|----------|
| Prompt 注入 | 系统提示隔离、用户输入标记、拒绝执行"忽略规则/越权访问/伪造身份"类指令 | 红队用例全通过 |
| RAG 越权 | 检索前强制注入 `client`、密级和字段权限过滤；引用跨租户文档直接阻断 | 越权召回率 = 0 |
| 工具越权 | Tool 权限矩阵按角色、模块、阶段控制；高风险 Tool（SQL、导出、外部推送）需审批或只读沙箱 | 工具调用审计完整 |
| 幻觉/事实性 | 法规、制度、证据和金额必须带来源引用；无法校验时标注"待人工核实" | 黄金评测集法规/证据引用准确率 ≥ 95% |
| 输出质量漂移 | 50 个标准 Prompt 扩展为模块黄金集；比较格式、结论、置信度、引用和人工评分 | 质量下降即阻断灰度 |
| 高风险自动化 | 涉刑、重大金额、机密下载、处罚执行和外部系统写入必须 HITL | 人工复核记录写入 audit_log |

---

### 8.23 第三方服务 SLA 要求

#### 8.23.1 依赖外部服务清单

| 服务 | 调用场景 | 可用性要求 | 延迟要求 | 降级策略 |
|------|----------|-----------|----------|----------|
| **Model Gateway providers** | 所有 LLM 推理（DeepSeek/Qwen/私有 endpoint） | 单 provider ≥ 99.0%；组合可用性 ≥ 99.5% | P95 < 15-20s | 自动熔断故障 provider，路由到备用 provider；全部不可用进入人工接管 |
| **Embedding API** | 文档向量化 + 查询向量化 | ≥ 99.5% | P95 < 500ms | 本地缓存 Embedding 结果 (7 天 TTL) |
| **风控系统** | 案件同步、状态回写 | ≥ 99.0% | P95 < 2s | Adapter 熔断器 + 本地队列缓冲 |
| **OA 系统 (BPM)** | 处罚公告审批 | ≥ 99.0% | P95 < 5s | 审批延后，手动补推 |
| **MDM 系统** | 黑名单同步、组织架构 | ≥ 99.5% | P95 < 2s | 本地缓存 (1h TTL) |
| **龟宝 (A2A)** | HR 案件转交 | ≥ 99.0% | P95 < 10s | 任务保留 a2a_queue，对方恢复后重试 |
| **西塞罗 (A2A)** | 法务案件推送 | ≥ 99.0% | P95 < 10s | 同上 |
| **波特 (A2A)** | 供应商扣款 | ≥ 99.0% | P95 < 10s | 同上 |
| **企查查 API** | 工商信息查询 | ≥ 99.0% | P95 < 3s | 本地缓存 (24h TTL) |

#### 8.23.2 外部依赖容错设计

```
外部调用模式 (每个外部系统独立 Adapter):

    API Layer
        │
        ▼
    Adapter.call(payload)
        │
        ├── [熔断器] CircuitBreaker(max_failures=5, cooldown=60s)
        │   ├── CLOSED → 正常调用
        │   ├── OPEN   → 直接返回降级结果 (5 次失败后)
        │   └── HALF_OPEN → 试探性调用 (60s 后)
        │
        ├── [重试] RetryPolicy(max_retries=3, backoff=exponential, max_delay=30s)
        │   └── 仅对幂等操作重试 (GET 查询 / 消息投递)
        │
        ├── [超时] Timeout(total=30s, connect=5s)
        │
        └── [降级] Fallback
            ├── 查询类: 返回缓存数据 + stale 标记
            ├── 写入类: 本地队列缓冲，外部恢复后补推
            └── LLM 类: 切换备用模型
```

#### 8.23.3 SLA 监控与违约处理

| 监控项 | 指标 | 告警阈值 | 违约处理 |
|--------|------|----------|----------|
| Model Gateway provider 组合可用率 | 30 天窗口 | < 99.5% | 启动 provider SLA 复盘，调整路由权重，加速私有模型 endpoint 容灾 |
| Embedding API 可用率 | 30 天窗口 | < 99.5% | 评估本地 Embedding 模型替代方案 |
| 外部系统可用率 | 30 天窗口 | < 99.0% | 与 IT 部门协调改进；提级到基础设施层面 |

---

### 8.24 最终生产架构默认假设与边界

本文档只描述最终生产架构，不把本地、测试、PoC 或演示环境作为生产架构的替代方案。非生产部署可以在独立部署手册中说明，但不得降低本文档定义的生产运行契约。

| 边界项 | 默认假设 |
|--------|----------|
| 部署平台 | 默认部署在 Kubernetes；状态型组件通过成熟 Operator 或 DBA 标准方案治理。 |
| 工作流权威 | LangGraph Workflow Runtime 是唯一流程推进中心，API、Agent、Worker 和外部系统不得直接跳转业务阶段。 |
| 持久化权威 | PostgreSQL 保存业务事实、审计事实、Outbox/Inbox 和 durable checkpoint；Redis 不作为关键 workflow state 唯一来源。 |
| 检索架构 | 全文检索、向量检索和业务数据库分属不同运行域，避免单一数据库承载 OLTP、审计、全文和向量全部负载。 |
| AI 执行边界 | AI 不直接执行处罚、移交、扣款、外部系统写入等高风险动作；所有高风险动作必须经过 HITL 和审计。 |
| 外部集成 | 所有外发事件走 Outbox，所有回调走 Inbox；必须验签、去重、记录 trace_id 和 audit_log。 |
| 可观测性 | OpenTelemetry 作为统一遥测标准；Prometheus/Grafana、LangFuse、Jaeger/Tempo、结构化日志以同一 trace_id 串联。 |
| 安全 | 生产密钥由 Vault/KMS + External Secrets Operator 注入；敏感字段列级加密，文件对象服务端加密，访问使用短期预签名 URL。 |

---

## 附录 A：文档修订历史

| 版本 | 日期 | 修订人 | 修订说明 |
|------|------|--------|----------|
| v1.0 | 2026-05 | — | 初始版本，基于总体需求文档 v1.1 和 technical-spec v1.0 编写，面向小团队原型 |
| v2.0 | 2026-05 | — | 重大升级：生产级架构，支持 10K 注册用户/10TB 数据/多模态处理。新增异步优先架构、数据分层、水平扩展、多模态管道、K8s 部署、Elasticsearch/RabbitMQ/Jaeger 等组件 |
| v2.3 | 2026-06 | — | 按最终生产项目口径重构架构边界：明确 LangGraph Workflow Runtime 为状态推进中心；PostgreSQL durable checkpoint；业务库与检索域拆分；Milvus 承载大规模向量检索；A2A 企业事件契约；OpenTelemetry 统一遥测；Model Gateway 多 provider 策略 |
| v2.2 | 2026-06 | — | 补齐 Patroni/pgBackRest、RabbitMQ quorum queues、网络边界、身份密钥治理、K8s 运行治理、供应链安全、AI 治理和消息契约 |
| v2.1 | 2026-06 | — | 补充生产环境缺失要素：新增成本估算 (§8.18)、知识库初始化策略 (§8.19)、应急预案 (§8.20)、开发与测试环境方案 (§8.21)、模型版本管理策略 (§8.22)、第三方服务 SLA 要求 (§8.23)、Docker Compose 测试方案 (§8.24)。新增参考文档 `doc/deployment-plan-100users.md` |

## 附录 B：v1.0 → v2.0 变更摘要

| 变更域 | v1.0 (原型) | v2.0 (生产级) |
|--------|------------|-------------|
| 目标规模 | 日均 50-200 案件，小团队 | 10,000 注册用户，峰值 500 并发，10TB 数据 |
| 部署 | Docker Compose 单机 | Kubernetes 多节点集群 (3 Master + 7 Worker) |
| 消息队列 | Celery + Redis (内存) | Celery + RabbitMQ quorum queues (持久化, DLX, 重放, 幂等消费) |
| 数据库 | PostgreSQL 单实例 | Patroni + Streaming Replication + PgBouncer + pgBackRest + WAL 归档 |
| 缓存 | Redis 单实例 | Redis 3主3从 Cluster |
| 存储 | MinIO 单节点 | MinIO 4节点 EC 12+4 + NAS冷归档 |
| 搜索 | PostgreSQL FTS | Elasticsearch/OpenSearch 全文检索 + Milvus Distributed 向量检索，均通过 Adapter 隔离 |
| 多模态 | 无专门管道 | 4条独立处理管道 (音频/图像/视频/文档) + GPU加速 |
| 数据生命周期 | 无 | 热温冷三层自动迁移 (90天/2年/10年) |
| 可观测性 | 三层 (LangFuse+Prometheus+structlog) | OpenTelemetry 统一采集 + Prometheus/Grafana + LangFuse + Jaeger/Tempo + 业务仪表板 |
| 工作流状态 | 无明确权威 | LangGraph Workflow Runtime + PostgreSQL durable checkpoint |
| 模型接入 | 单一模型调用 | Model Gateway + Provider Adapter + Model Registry + Routing Policy |
| ADR | 4 项 | 11 项 |
| 可用性 | 无明确目标 | 生产 API 99.9%，DB 主库故障 RPO < 5min / RTO < 15min；非生产环境不承诺生产 SLA |

## 附录 C：参考文档

| 文档 | 路径 |
|------|------|
| 总体需求文档 | `doc/hermes-requirements.md` |
| 详细实施规范 | `doc/technical-spec.md` |
| 廉洁监察需求 | `doc/科沃斯-赫尔墨斯 AI 需求文档-廉洁监察.pdf` |
| 风险监控需求 | `doc/科沃斯-赫尔墨斯AI需求文档-风险监控V3.pdf` |
| 内控评价需求 | `doc/科沃斯-赫尔墨斯AI需求文档-内控评价.pdf` |
| 专项审计需求 | `doc/科沃斯-赫尔墨斯AI需求文档-专项审计.pdf` |
| 离任审计需求 | `doc/科沃斯-赫尔墨斯AI需求文档-离任审计.pdf` |
| 商业秘密需求 | `doc/科沃斯-赫尔墨斯AI需求文档-商业秘密.pdf` |
| 行为风险需求 | `doc/科沃斯-赫尔墨斯AI需求文档-行为风险.pdf` |
| 持续改善需求 | `doc/科沃斯-赫尔墨斯AI需求文档-持续改善.pdf` |
| 非生产本地/测试部署方案 | `doc/deployment-plan-100users.md` |
| 数据设计文档 | `doc/data-design.md` |
| API 设计文档 | `doc/api-design.md` |
| 模块需求文档 | `doc/modules/` |
